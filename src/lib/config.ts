/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as models from "../models/index.js";
import { HTTPClient } from "./http.js";
import { Logger } from "./logger.js";
import { RetryConfig } from "./retries.js";
import { pathToFunc } from "./url.js";

export type SDKOptions = {
  /**
   * The security details required to authenticate the SDK
   */
  security?: models.Security | (() => Promise<models.Security>) | undefined;

  httpClient?: HTTPClient;
  /**
   * Specifies the server URL to be used by the SDK
   */
  serverURL: string;
  /**
   * Allows overriding the default user agent used by the SDK
   */
  userAgent?: string | undefined;
  /**
   * Allows overriding the default retry config used by the SDK
   */
  retryConfig?: RetryConfig;
  timeoutMs?: number;
  debugLogger?: Logger;
};

export function serverURLFromOptions(options: SDKOptions): URL | null {
  const serverURL = options.serverURL;

  if (!serverURL) {
    return null;
  }

  const params: Record<string, string | undefined> = {};

  const u = pathToFunc(serverURL)(params);
  return new URL(u);
}

export const SDK_METADATA = {
  language: "typescript",
  openapiDocVersion: "4.17.1-alpha.1773443034117-eed37cfb",
  sdkVersion: "0.7.0-alpha.7",
  genVersion: "2.862.0",
  userAgent:
    "speakeasy-sdk/typescript 0.7.0-alpha.7 2.862.0 4.17.1-alpha.1773443034117-eed37cfb cribl-control-plane",
} as const;
