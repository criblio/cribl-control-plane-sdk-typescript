/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: e91c01d19709
 */

import { nodesGet } from "../../funcs/nodesGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetProductsWorkersByProductAndIdRequest$inboundSchema,
};

export const tool$nodesGet: ToolDefinition<typeof args> = {
  name: "nodes-get",
  description: `Get detailed metadata for a Worker or Edge Node

Get detailed metadata for the specified Worker or Edge Node for the specified Cribl product.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await nodesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
