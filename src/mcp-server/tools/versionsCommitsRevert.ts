/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: b4ba8c9a976f
 */

import { versionsCommitsRevert } from "../../funcs/versionsCommitsRevert.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.GitRevertParams$inboundSchema,
};

export const tool$versionsCommitsRevert: ToolDefinition<typeof args> = {
  name: "versions-commits-revert",
  description: `Revert a commit in the local repository

Revert a commit in the local repository.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await versionsCommitsRevert(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
