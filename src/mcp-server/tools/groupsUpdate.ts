/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { groupsUpdate } from "../../funcs/groupsUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateConfigGroupByProductAndIdRequest$inboundSchema,
};

export const tool$groupsUpdate: ToolDefinition<typeof args> = {
  name: "groups-update",
  description: `Update a Worker Group, Outpost Group, or Edge Fleet

Update the specified Worker Group, Outpost Group, or Edge Fleet.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await groupsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
