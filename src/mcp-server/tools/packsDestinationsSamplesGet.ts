/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { packsDestinationsSamplesGet } from "../../funcs/packsDestinationsSamplesGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetOutputSystemSamplesByPackAndIdRequest$inboundSchema,
};

export const tool$packsDestinationsSamplesGet: ToolDefinition<typeof args> = {
  name: "packs-destinations-samples-get",
  description: `Get sample event data for a Destination within a Pack

Get sample event data for the specified Destination to validate the configuration or test connectivity within the specified Pack.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await packsDestinationsSamplesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
