/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: e04c03314c5f
 */

import { packsDestinationsSamplesCreate } from "../../funcs/packsDestinationsSamplesCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateOutputSystemTestByPackAndIdRequest$inboundSchema,
};

export const tool$packsDestinationsSamplesCreate: ToolDefinition<typeof args> =
  {
    name: "packs-destinations-samples-create",
    description: `Send sample event data to a Destination within a Pack

Send sample event data to the specified Destination to validate the configuration or test connectivity within the specified Pack.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await packsDestinationsSamplesCreate(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
