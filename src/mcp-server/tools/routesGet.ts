/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { routesGet } from "../../funcs/routesGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetRoutesByIdRequest$inboundSchema,
};

export const tool$routesGet: ToolDefinition<typeof args> = {
  name: "routes-get",
  description: `Get a Routing table

Get the specified Routing table.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await routesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
