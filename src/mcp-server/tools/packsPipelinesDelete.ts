/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 881c9f5147fa
 */

import { packsPipelinesDelete } from "../../funcs/packsPipelinesDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeletePipelinesByPackAndIdRequest$inboundSchema,
};

export const tool$packsPipelinesDelete: ToolDefinition<typeof args> = {
  name: "packs-pipelines-delete",
  description: `Delete a Pipeline within a Pack

Delete the specified Pipeline within the specified Pack.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await packsPipelinesDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
