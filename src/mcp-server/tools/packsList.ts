/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { packsList } from "../../funcs/packsList.js";
import * as operations from "../../models/operations/index.js";
import * as types from "../../types/primitives.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: types.optional(operations.GetPacksRequest$inboundSchema),
};

export const tool$packsList: ToolDefinition<typeof args> = {
  name: "packs-list",
  description: `List all Packs

Get a list of all Packs.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await packsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
