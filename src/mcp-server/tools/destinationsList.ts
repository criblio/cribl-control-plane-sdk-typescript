/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: dc4faba51af3
 */

import { destinationsList } from "../../funcs/destinationsList.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$destinationsList: ToolDefinition = {
  name: "destinations-list",
  description: `List all Destinations

Get a list of all Destinations.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await destinationsList(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
