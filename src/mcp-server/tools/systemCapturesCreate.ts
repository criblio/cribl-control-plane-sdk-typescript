/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 37de742943a9
 */

import { systemCapturesCreate } from "../../funcs/systemCapturesCreate.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.CaptureParams$inboundSchema,
};

export const tool$systemCapturesCreate: ToolDefinition<typeof args> = {
  name: "system-captures-create",
  description: `Capture live incoming data

Initiate a live data capture from Cribl Workers.Returns a stream of captured events in NDJSON format that match the parameters specified in the request body.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await systemCapturesCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
