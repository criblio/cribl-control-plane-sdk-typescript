/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { databaseConnectionsDelete } from "../../funcs/databaseConnectionsDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteDatabaseConnectionConfigByIdRequest$inboundSchema,
};

export const tool$databaseConnectionsDelete: ToolDefinition<typeof args> = {
  name: "database-connections-delete",
  description: `Delete a Database Connection

Delete the specified Database Connection.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await databaseConnectionsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
