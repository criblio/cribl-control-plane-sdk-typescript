/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { versionsCommitsFilesCount } from "../../funcs/versionsCommitsFilesCount.js";
import * as operations from "../../models/operations/index.js";
import * as types from "../../types/primitives.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: types.optional(operations.GetVersionCountRequest$inboundSchema),
};

export const tool$versionsCommitsFilesCount: ToolDefinition<typeof args> = {
  name: "versions-commits-files-count",
  description: `Get a count of files that changed since a commit

Get a count of the files that changed since a commit. Default is the latest commit (HEAD).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await versionsCommitsFilesCount(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
