/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { systemSettingsCriblUpdate } from "../../funcs/systemSettingsCriblUpdate.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.SystemSettingsConf$inboundSchema,
};

export const tool$systemSettingsCriblUpdate: ToolDefinition<typeof args> = {
  name: "system-settings-cribl-update",
  description: `Update Cribl system settings

Update Cribl system settings.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await systemSettingsCriblUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
