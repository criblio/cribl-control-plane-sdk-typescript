/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { authTokensGet } from "../../funcs/authTokensGet.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.LoginInfo$inboundSchema,
};

export const tool$authTokensGet: ToolDefinition<typeof args> = {
  name: "auth-tokens-get",
  description: `Log in and fetch an authentication token

This endpoint is unavailable on Cribl.Cloud.Instead, follow the instructions at https://docs.cribl.io/stream/api-tutorials/#criblcloud to get an Auth token for Cribl.Cloud.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await authTokensGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
