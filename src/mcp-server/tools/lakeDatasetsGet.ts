/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lakeDatasetsGet } from "../../funcs/lakeDatasetsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCriblLakeDatasetByLakeIdAndIdRequest$inboundSchema,
};

export const tool$lakeDatasetsGet: ToolDefinition<typeof args> = {
  name: "lake-datasets-get",
  description: `Get a Lake Dataset (Cribl.Cloud only)

Get the specified Lake Dataset in the specified Lake (Cribl.Cloud only).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lakeDatasetsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
