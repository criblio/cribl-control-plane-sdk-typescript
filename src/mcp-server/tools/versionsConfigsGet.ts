/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { versionsConfigsGet } from "../../funcs/versionsConfigsGet.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$versionsConfigsGet: ToolDefinition = {
  name: "versions-configs-get",
  description: `Get the configuration and status for the Git integration

Get the configuration and versioning status for the Git integration for the Cribl configuration.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await versionsConfigsGet(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
