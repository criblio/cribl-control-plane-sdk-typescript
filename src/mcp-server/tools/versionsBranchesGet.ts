/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: d0f1fb8d695e
 */

import { versionsBranchesGet } from "../../funcs/versionsBranchesGet.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$versionsBranchesGet: ToolDefinition = {
  name: "versions-branches-get",
  description:
    `Get the name of the Git branch that the Cribl configuration is checked out to

Get the name of the Git branch that the Cribl configuration is checked out to. Useful for verifying the active configuration branch.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await versionsBranchesGet(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
