/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { destinationsStatusesGet } from "../../funcs/destinationsStatusesGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetOutputStatusByIdRequest$inboundSchema,
};

export const tool$destinationsStatusesGet: ToolDefinition<typeof args> = {
  name: "destinations-statuses-get",
  description: `Get the status of a Destination

Get the status and optional metrics for the specified Destination.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await destinationsStatusesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
