/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 9d26522ff73d
 */

import { packsSourcesHecTokensUpdate } from "../../funcs/packsSourcesHecTokensUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations
      .UpdateInputSystemHecTokenByPackAndIdAndTokenRequest$inboundSchema,
};

export const tool$packsSourcesHecTokensUpdate: ToolDefinition<typeof args> = {
  name: "packs-sources-hec-tokens-update",
  description:
    `Update metadata for an HEC token for a Splunk HEC Source within a Pack

Update the metadata for the specified HEC token for the specified Splunk HEC Source within the specified Pack.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await packsSourcesHecTokensUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
