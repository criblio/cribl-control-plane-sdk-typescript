/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lakeDatasetsDelete } from "../../funcs/lakeDatasetsDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteCriblLakeDatasetByLakeIdAndIdRequest$inboundSchema,
};

export const tool$lakeDatasetsDelete: ToolDefinition<typeof args> = {
  name: "lake-datasets-delete",
  description: `Delete a Lake Dataset (Cribl.Cloud only)

Delete the specified Lake Dataset in the specified Lake (Cribl.Cloud only).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lakeDatasetsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
