/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 01c067c71728
 */

import { functionsGet } from "../../funcs/functionsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetFunctionsByIdRequest$inboundSchema,
};

export const tool$functionsGet: ToolDefinition<typeof args> = {
  name: "functions-get",
  description: `Get a Function

Get the specified Function.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await functionsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
