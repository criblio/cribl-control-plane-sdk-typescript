/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { systemSettingsCriblList } from "../../funcs/systemSettingsCriblList.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$systemSettingsCriblList: ToolDefinition = {
  name: "system-settings-cribl-list",
  description: `Get Cribl system settings

Get Cribl system settings.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await systemSettingsCriblList(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
