/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { destinationsStatusesList } from "../../funcs/destinationsStatusesList.js";
import * as operations from "../../models/operations/index.js";
import * as types from "../../types/primitives.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: types.optional(operations.GetOutputStatusRequest$inboundSchema),
};

export const tool$destinationsStatusesList: ToolDefinition<typeof args> = {
  name: "destinations-statuses-list",
  description: `List the status of all Destinations

Get a list of status information and optional health metrics for all configured Destinations in the Worker Group or Edge Fleet.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await destinationsStatusesList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
