/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { groupsGet } from "../../funcs/groupsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetConfigGroupByProductAndIdRequest$inboundSchema,
};

export const tool$groupsGet: ToolDefinition<typeof args> = {
  name: "groups-get",
  description: `Get a Worker Group, Outpost Group, or Edge Fleet

Get the specified Worker Group, Outpost Group, or Edge Fleet.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await groupsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
