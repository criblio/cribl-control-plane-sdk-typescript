/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { packsSourcesHecTokensCreate } from "../../funcs/packsSourcesHecTokensCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateInputSystemHecTokenByPackAndIdRequest$inboundSchema,
};

export const tool$packsSourcesHecTokensCreate: ToolDefinition<typeof args> = {
  name: "packs-sources-hec-tokens-create",
  description:
    `Add an HEC token and optional metadata to a Splunk HEC Source within a Pack

Add an HEC token and optional metadata to the specified Splunk HEC Source within the specified Pack.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await packsSourcesHecTokensCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
