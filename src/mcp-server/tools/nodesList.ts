/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { nodesList } from "../../funcs/nodesList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetProductsWorkersByProductRequest$inboundSchema,
};

export const tool$nodesList: ToolDefinition<typeof args> = {
  name: "nodes-list",
  description: `Get detailed metadata for Worker or Edge Nodes

Get detailed metadata for Worker or Edge Nodes for the specified Cribl product.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await nodesList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
