/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: ded19b2f3975
 */

import { sourcesHecTokensCreate } from "../../funcs/sourcesHecTokensCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateInputHecTokenByIdRequest$inboundSchema,
};

export const tool$sourcesHecTokensCreate: ToolDefinition<typeof args> = {
  name: "sources-hec-tokens-create",
  description: `Add an HEC token and optional metadata to a Splunk HEC Source

Add an HEC token and optional metadata to the specified Splunk HEC Source.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sourcesHecTokensCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
