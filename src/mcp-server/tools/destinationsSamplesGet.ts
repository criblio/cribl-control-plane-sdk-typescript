/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 38b9e15dcff9
 */

import { destinationsSamplesGet } from "../../funcs/destinationsSamplesGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetOutputSamplesByIdRequest$inboundSchema,
};

export const tool$destinationsSamplesGet: ToolDefinition<typeof args> = {
  name: "destinations-samples-get",
  description: `Get sample event data for a Destination

Get sample event data for the specified Destination to validate the configuration or test connectivity.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await destinationsSamplesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
