/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { packsDestinationsPqClear } from "../../funcs/packsDestinationsPqClear.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteOutputSystemPqByPackAndIdRequest$inboundSchema,
};

export const tool$packsDestinationsPqClear: ToolDefinition<typeof args> = {
  name: "packs-destinations-pq-clear",
  description: `Clear the persistent queue for a Destination within a Pack

Clear the persistent queue (PQ) for the specified Destination within the specified Pack.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await packsDestinationsPqClear(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
