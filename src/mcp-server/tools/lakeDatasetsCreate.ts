/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: ba6710be7f40
 */

import { lakeDatasetsCreate } from "../../funcs/lakeDatasetsCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateCriblLakeDatasetByLakeIdRequest$inboundSchema,
};

export const tool$lakeDatasetsCreate: ToolDefinition<typeof args> = {
  name: "lake-datasets-create",
  description: `Create a Lake Dataset (Cribl.Cloud only)

Create a new Lake Dataset in the specified Lake (Cribl.Cloud only).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lakeDatasetsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
