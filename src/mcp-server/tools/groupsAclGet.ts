/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: feeefb59f5eb
 */

import { groupsAclGet } from "../../funcs/groupsAclGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetConfigGroupAclByProductAndIdRequest$inboundSchema,
};

export const tool$groupsAclGet: ToolDefinition<typeof args> = {
  name: "groups-acl-get",
  description:
    `Get the Access Control List for a Worker Group, Outpost Group, or Edge Fleet

Get the Access Control List (ACL) for the specified Worker Group, Outpost Group, or Edge Fleet.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await groupsAclGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
