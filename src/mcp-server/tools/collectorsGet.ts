/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: f12d65f9c471
 */

import { collectorsGet } from "../../funcs/collectorsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetSavedJobByIdRequest$inboundSchema,
};

export const tool$collectorsGet: ToolDefinition<typeof args> = {
  name: "collectors-get",
  description: `Get a Collector

Get the specified Collector.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await collectorsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
