/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { groupsConfigsVersionsGet } from "../../funcs/groupsConfigsVersionsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.GetConfigGroupConfigVersionByProductAndIdRequest$inboundSchema,
};

export const tool$groupsConfigsVersionsGet: ToolDefinition<typeof args> = {
  name: "groups-configs-versions-get",
  description:
    `Get the configuration version for a Worker Group, Outpost Group, or Edge Fleet

Get the configuration version for the specified Worker Group, Outpost Group, or Edge Fleet.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await groupsConfigsVersionsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
