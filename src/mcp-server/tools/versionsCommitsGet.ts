/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { versionsCommitsGet } from "../../funcs/versionsCommitsGet.js";
import * as operations from "../../models/operations/index.js";
import * as types from "../../types/primitives.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: types.optional(operations.GetVersionShowRequest$inboundSchema),
};

export const tool$versionsCommitsGet: ToolDefinition<typeof args> = {
  name: "versions-commits-get",
  description: `Get the diff and log message for a commit

Get the diff and log message for a commit. Default is the latest commit (HEAD).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await versionsCommitsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
