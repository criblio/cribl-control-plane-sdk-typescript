/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: cf80d21a9a96
 */

import { packsSourcesPqClear } from "../../funcs/packsSourcesPqClear.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteInputSystemPqByPackAndIdRequest$inboundSchema,
};

export const tool$packsSourcesPqClear: ToolDefinition<typeof args> = {
  name: "packs-sources-pq-clear",
  description: `Clear the persistent queue for a Source within a Pack

Clear the persistent queue (PQ) for the specified Source within the specified Pack.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await packsSourcesPqClear(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
