/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sourcesHecTokensUpdate } from "../../funcs/sourcesHecTokensUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateInputHecTokenByIdAndTokenRequest$inboundSchema,
};

export const tool$sourcesHecTokensUpdate: ToolDefinition<typeof args> = {
  name: "sources-hec-tokens-update",
  description: `Update metadata for an HEC token for a Splunk HEC Source

Update the metadata for the specified HEC token for the specified Splunk HEC Source.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sourcesHecTokensUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
