/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sourcesDelete } from "../../funcs/sourcesDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteInputByIdRequest$inboundSchema,
};

export const tool$sourcesDelete: ToolDefinition<typeof args> = {
  name: "sources-delete",
  description: `Delete a Source

Delete the specified Source.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sourcesDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
