/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { destinationsSamplesCreate } from "../../funcs/destinationsSamplesCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateOutputTestByIdRequest$inboundSchema,
};

export const tool$destinationsSamplesCreate: ToolDefinition<typeof args> = {
  name: "destinations-samples-create",
  description: `Send sample event data to a Destination

Send sample event data to the specified Destination to validate the configuration or test connectivity.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await destinationsSamplesCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
