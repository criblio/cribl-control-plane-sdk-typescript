/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { databaseConnectionsCreate } from "../../funcs/databaseConnectionsCreate.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.DatabaseConnectionConfig$inboundSchema,
};

export const tool$databaseConnectionsCreate: ToolDefinition<typeof args> = {
  name: "database-connections-create",
  description: `Create Database Connection

Create a new Database Connection.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await databaseConnectionsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
