/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sourcesStatusesList } from "../../funcs/sourcesStatusesList.js";
import * as operations from "../../models/operations/index.js";
import * as types from "../../types/primitives.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: types.optional(operations.GetInputStatusRequest$inboundSchema),
};

export const tool$sourcesStatusesList: ToolDefinition<typeof args> = {
  name: "sources-statuses-list",
  description: `List the status of all Sources

Get a list of status information and optional metrics for all configured Sources in the Worker Group or Edge Fleet.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sourcesStatusesList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
