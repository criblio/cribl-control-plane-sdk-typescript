/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { packsRoutesAppend } from "../../funcs/packsRoutesAppend.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateRoutesAppendByPackAndIdRequest$inboundSchema,
};

export const tool$packsRoutesAppend: ToolDefinition<typeof args> = {
  name: "packs-routes-append",
  description: `Add a Route to the end of the Routing table within a Pack

Add a Route to the end of the specified Routing table within the specified Pack.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await packsRoutesAppend(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
