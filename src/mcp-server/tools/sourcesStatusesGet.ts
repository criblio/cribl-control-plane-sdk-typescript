/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sourcesStatusesGet } from "../../funcs/sourcesStatusesGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetInputStatusByIdRequest$inboundSchema,
};

export const tool$sourcesStatusesGet: ToolDefinition<typeof args> = {
  name: "sources-statuses-get",
  description: `Get the status of a Source

Get the status and optional metrics for the specified Source.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sourcesStatusesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
