/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 32acd195d0e4
 */

import { pipelinesCreate } from "../../funcs/pipelinesCreate.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.PipelineInput$inboundSchema,
};

export const tool$pipelinesCreate: ToolDefinition<typeof args> = {
  name: "pipelines-create",
  description: `Create a Pipeline

Create a new Pipeline.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await pipelinesCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
