/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: d3a47121033b
 */

import { collectorsList } from "../../funcs/collectorsList.js";
import * as operations from "../../models/operations/index.js";
import * as types from "../../types/primitives.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: types.optional(operations.GetSavedJobRequest$inboundSchema),
};

export const tool$collectorsList: ToolDefinition<typeof args> = {
  name: "collectors-list",
  description: `List all Collectors

Get a list of all Collectors.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await collectorsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
