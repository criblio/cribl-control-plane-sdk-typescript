/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { destinationsPqGet } from "../../funcs/destinationsPqGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetOutputPqByIdRequest$inboundSchema,
};

export const tool$destinationsPqGet: ToolDefinition<typeof args> = {
  name: "destinations-pq-get",
  description:
    `Get information about the latest job to clear the persistent queue for a Destination

Get information about the latest job to clear the persistent queue (PQ) for the specified Destination.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await destinationsPqGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
