/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: e39246e0b2ce
 */

import { pipelinesGet } from "../../funcs/pipelinesGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetPipelinesByIdRequest$inboundSchema,
};

export const tool$pipelinesGet: ToolDefinition<typeof args> = {
  name: "pipelines-get",
  description: `Get a Pipeline

Get the specified Pipeline.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await pipelinesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
