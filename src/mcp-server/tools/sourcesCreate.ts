/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 0a95b8c9e674
 */

import { sourcesCreate } from "../../funcs/sourcesCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateInputRequest$inboundSchema,
};

export const tool$sourcesCreate: ToolDefinition<typeof args> = {
  name: "sources-create",
  description: `Create a Source

Create a new Source.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sourcesCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
