/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 10218cf22ea6
 */

import { packsSourcesStatusesList } from "../../funcs/packsSourcesStatusesList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetInputStatusSystemInputsByPackRequest$inboundSchema,
};

export const tool$packsSourcesStatusesList: ToolDefinition<typeof args> = {
  name: "packs-sources-statuses-list",
  description: `List the status of all Sources within a Pack

Get a list of status information and optional metrics for all configured Sources in the Worker Group or Edge Fleet within the specified Pack.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await packsSourcesStatusesList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
