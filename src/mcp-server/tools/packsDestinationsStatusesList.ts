/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 7efb2b48a5b7
 */

import { packsDestinationsStatusesList } from "../../funcs/packsDestinationsStatusesList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetOutputStatusSystemOutputsByPackRequest$inboundSchema,
};

export const tool$packsDestinationsStatusesList: ToolDefinition<typeof args> = {
  name: "packs-destinations-statuses-list",
  description: `List the status of all Destinations within a Pack

Get a list of status information and optional health metrics for all configured Destinations in the Worker Group or Edge Fleet within the specified Pack.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await packsDestinationsStatusesList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
