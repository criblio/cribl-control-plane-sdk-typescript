/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: ea9e18e406a7
 */

import { sourcesGet } from "../../funcs/sourcesGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetInputByIdRequest$inboundSchema,
};

export const tool$sourcesGet: ToolDefinition<typeof args> = {
  name: "sources-get",
  description: `Get a Source

Get the specified Source.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sourcesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
