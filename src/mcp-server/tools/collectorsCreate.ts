/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: f8b56c14fd6c
 */

import { collectorsCreate } from "../../funcs/collectorsCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateSavedJobRequest$inboundSchema,
};

export const tool$collectorsCreate: ToolDefinition<typeof args> = {
  name: "collectors-create",
  description: `Create a Collector

Create a new Collector.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await collectorsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
