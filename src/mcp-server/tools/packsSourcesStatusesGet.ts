/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { packsSourcesStatusesGet } from "../../funcs/packsSourcesStatusesGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.GetInputStatusSystemInputsByPackAndIdRequest$inboundSchema,
};

export const tool$packsSourcesStatusesGet: ToolDefinition<typeof args> = {
  name: "packs-sources-statuses-get",
  description: `Get the status of a Source within a Pack

Get the status and optional metrics for the specified Source within the specified Pack.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await packsSourcesStatusesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
