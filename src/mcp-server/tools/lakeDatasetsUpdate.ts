/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lakeDatasetsUpdate } from "../../funcs/lakeDatasetsUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateCriblLakeDatasetByLakeIdAndIdRequest$inboundSchema,
};

export const tool$lakeDatasetsUpdate: ToolDefinition<typeof args> = {
  name: "lake-datasets-update",
  description: `Update a Lake Dataset (Cribl.Cloud only)

Update the specified Lake Dataset in the specified Lake (Cribl.Cloud only).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lakeDatasetsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
