/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 1a37a329ab22
 */

import { packsDestinationsDelete } from "../../funcs/packsDestinationsDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteOutputSystemByPackAndIdRequest$inboundSchema,
};

export const tool$packsDestinationsDelete: ToolDefinition<typeof args> = {
  name: "packs-destinations-delete",
  description: `Delete a Destination within a Pack

Delete the specified Destination within the specified Pack.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await packsDestinationsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
