/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lakeDatasetsList } from "../../funcs/lakeDatasetsList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCriblLakeDatasetByLakeIdRequest$inboundSchema,
};

export const tool$lakeDatasetsList: ToolDefinition<typeof args> = {
  name: "lake-datasets-list",
  description: `List all Lake Datasets (Cribl.Cloud only)

Get a list of all Lake Datasets in the specified Lake (Cribl.Cloud only).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lakeDatasetsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
