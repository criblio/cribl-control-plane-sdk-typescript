/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { collectorsDelete } from "../../funcs/collectorsDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteSavedJobByIdRequest$inboundSchema,
};

export const tool$collectorsDelete: ToolDefinition<typeof args> = {
  name: "collectors-delete",
  description: `Delete a Collector

Delete the specified Collector.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await collectorsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
