/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 18ee74ba4e67
 */

import { packsSourcesPqGet } from "../../funcs/packsSourcesPqGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetInputSystemPqByPackAndIdRequest$inboundSchema,
};

export const tool$packsSourcesPqGet: ToolDefinition<typeof args> = {
  name: "packs-sources-pq-get",
  description:
    `Get information about the latest job to clear the persistent queue for a Source within a Pack

Get information about the latest job to clear the persistent queue (PQ) for the specified Source within the specified Pack.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await packsSourcesPqGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
