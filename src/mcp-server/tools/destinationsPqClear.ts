/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 978588430d97
 */

import { destinationsPqClear } from "../../funcs/destinationsPqClear.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteOutputPqByIdRequest$inboundSchema,
};

export const tool$destinationsPqClear: ToolDefinition<typeof args> = {
  name: "destinations-pq-clear",
  description: `Clear the persistent queue for a Destination

Clear the persistent queue (PQ) for the specified Destination.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await destinationsPqClear(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
