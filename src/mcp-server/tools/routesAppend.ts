/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { routesAppend } from "../../funcs/routesAppend.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateRoutesAppendByIdRequest$inboundSchema,
};

export const tool$routesAppend: ToolDefinition<typeof args> = {
  name: "routes-append",
  description: `Add a Route to the end of the Routing table

Add a Route to the end of the specified Routing table.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await routesAppend(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
