/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 5a6687ff72ba
 */

import { sourcesPqClear } from "../../funcs/sourcesPqClear.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteInputPqByIdRequest$inboundSchema,
};

export const tool$sourcesPqClear: ToolDefinition<typeof args> = {
  name: "sources-pq-clear",
  description: `Clear the persistent queue for a Source

Clear the persistent queue (PQ) for the specified Source.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sourcesPqClear(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
