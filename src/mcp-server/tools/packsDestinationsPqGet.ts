/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 98f94bb1069d
 */

import { packsDestinationsPqGet } from "../../funcs/packsDestinationsPqGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetOutputSystemPqByPackAndIdRequest$inboundSchema,
};

export const tool$packsDestinationsPqGet: ToolDefinition<typeof args> = {
  name: "packs-destinations-pq-get",
  description:
    `Get information about the latest job to clear the persistent queue for a Destination within a Pack

Get information about the latest job to clear the persistent queue (PQ) for the specified Destination within the specified Pack.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await packsDestinationsPqGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
