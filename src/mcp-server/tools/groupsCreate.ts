/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { groupsCreate } from "../../funcs/groupsCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateConfigGroupByProductRequest$inboundSchema,
};

export const tool$groupsCreate: ToolDefinition<typeof args> = {
  name: "groups-create",
  description:
    `Create a Worker Group, Outpost Group, or Edge Fleet for the specified Cribl product

Create a new Worker Group, Outpost Group, or Edge Fleet for the specified Cribl product.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await groupsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
