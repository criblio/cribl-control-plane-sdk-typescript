/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { databaseConnectionsGet } from "../../funcs/databaseConnectionsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetDatabaseConnectionConfigByIdRequest$inboundSchema,
};

export const tool$databaseConnectionsGet: ToolDefinition<typeof args> = {
  name: "database-connections-get",
  description: `Get a Database Connection

Get the specified Database Connection.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await databaseConnectionsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
