/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: cdd698d87df6
 */

import { packsDestinationsStatusesGet } from "../../funcs/packsDestinationsStatusesGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.GetOutputStatusSystemOutputsByPackAndIdRequest$inboundSchema,
};

export const tool$packsDestinationsStatusesGet: ToolDefinition<typeof args> = {
  name: "packs-destinations-statuses-get",
  description: `Get the status of a Destination within a Pack

Get the status and optional metrics for the specified Destination within the specified Pack.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await packsDestinationsStatusesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
