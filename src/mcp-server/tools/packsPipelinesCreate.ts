/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { packsPipelinesCreate } from "../../funcs/packsPipelinesCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreatePipelinesByPackRequest$inboundSchema,
};

export const tool$packsPipelinesCreate: ToolDefinition<typeof args> = {
  name: "packs-pipelines-create",
  description: `Create a Pipeline within a Pack

Create a new Pipeline within the specified Pack.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await packsPipelinesCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
