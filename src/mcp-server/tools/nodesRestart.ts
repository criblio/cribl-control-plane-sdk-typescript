/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 990bf6a5e2f5
 */

import { nodesRestart } from "../../funcs/nodesRestart.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.UpdateProductsWorkersRestartByProductRequest$inboundSchema,
};

export const tool$nodesRestart: ToolDefinition<typeof args> = {
  name: "nodes-restart",
  description: `Restart Worker or Edge Nodes

Restart all Worker or Edge Nodes for the specified Cribl product.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await nodesRestart(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
