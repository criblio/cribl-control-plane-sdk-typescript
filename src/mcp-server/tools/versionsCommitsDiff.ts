/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { versionsCommitsDiff } from "../../funcs/versionsCommitsDiff.js";
import * as operations from "../../models/operations/index.js";
import * as types from "../../types/primitives.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: types.optional(operations.GetVersionDiffRequest$inboundSchema),
};

export const tool$versionsCommitsDiff: ToolDefinition<typeof args> = {
  name: "versions-commits-diff",
  description: `Get the diff for a commit

Get the diff for a commit. Default is the latest commit (HEAD).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await versionsCommitsDiff(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
