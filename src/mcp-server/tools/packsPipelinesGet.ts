/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { packsPipelinesGet } from "../../funcs/packsPipelinesGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetPipelinesByPackAndIdRequest$inboundSchema,
};

export const tool$packsPipelinesGet: ToolDefinition<typeof args> = {
  name: "packs-pipelines-get",
  description: `Get a Pipeline within a Pack

Get the specified Pipeline within the specified Pack.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await packsPipelinesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
