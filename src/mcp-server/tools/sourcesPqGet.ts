/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: adb2e94d10e5
 */

import { sourcesPqGet } from "../../funcs/sourcesPqGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetInputPqByIdRequest$inboundSchema,
};

export const tool$sourcesPqGet: ToolDefinition<typeof args> = {
  name: "sources-pq-get",
  description:
    `Get information about the latest job to clear the persistent queue for a Source

Get information about the latest job to clear the persistent queue (PQ) for the specified Source.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sourcesPqGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
