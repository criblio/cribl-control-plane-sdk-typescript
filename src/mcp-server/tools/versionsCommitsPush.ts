/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: ae1f34e22d2a
 */

import { versionsCommitsPush } from "../../funcs/versionsCommitsPush.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$versionsCommitsPush: ToolDefinition = {
  name: "versions-commits-push",
  description: `Push local commits to the remote repository

Push all local commits from the local repository to the remote repository.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await versionsCommitsPush(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
