/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { groupsDeploy } from "../../funcs/groupsDeploy.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.UpdateConfigGroupDeployByProductAndIdRequest$inboundSchema,
};

export const tool$groupsDeploy: ToolDefinition<typeof args> = {
  name: "groups-deploy",
  description: `Deploy commits to a Worker Group, Outpost Group, or Edge Fleet

Deploy commits to the specified Worker Group, Outpost Group, or Edge Fleet.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await groupsDeploy(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
