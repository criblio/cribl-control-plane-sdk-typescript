/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 8b1bc5b2f16e
 */

import { destinationsCreate } from "../../funcs/destinationsCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateOutputRequest$inboundSchema,
};

export const tool$destinationsCreate: ToolDefinition<typeof args> = {
  name: "destinations-create",
  description: `Create a Destination

Create a new Destination.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await destinationsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
