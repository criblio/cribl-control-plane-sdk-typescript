/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import {
  CaptureLevel,
  CaptureLevel$inboundSchema,
  CaptureLevel$outboundSchema,
} from "./capturelevel.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type CaptureParams = {
  /**
   * Amount of time to keep capture open, in seconds.
   */
  duration: number;
  /**
   * JavaScript expression evaluated against each event to determine whether an event is included in the capture output. Expressions can reference any event field and use logical operators.
   */
  filter: string;
  /**
   * Stage at which events are captured. <br><code>0</code> == Before pre-processing Pipeline <br><code>1</code> == Before the Routes <br><code>2</code> == Before post-processing Pipeline <br><code>3</code> == Before the Destination.
   */
  level: CaptureLevel;
  /**
   * Maximum number of events to capture.
   */
  maxEvents: number;
  /**
   * How long to wait before increasing the capture sample size. Specify <code>1</code> second or longer.
   */
  stepDuration?: number | undefined;
  /**
   * Unique ID of the Worker.
   */
  workerId?: string | undefined;
  /**
   * Maximum number of Workers that can capture initially. A value of <code>0</code> means unlimited (all available Workers can capture).
   */
  workerThreshold?: number | undefined;
};

/** @internal */
export const CaptureParams$inboundSchema: z.ZodType<
  CaptureParams,
  z.ZodTypeDef,
  unknown
> = z.object({
  duration: types.number(),
  filter: types.string(),
  level: CaptureLevel$inboundSchema,
  maxEvents: types.number(),
  stepDuration: types.optional(types.number()),
  workerId: types.optional(types.string()),
  workerThreshold: types.optional(types.number()),
});
/** @internal */
export type CaptureParams$Outbound = {
  duration: number;
  filter: string;
  level: number;
  maxEvents: number;
  stepDuration?: number | undefined;
  workerId?: string | undefined;
  workerThreshold?: number | undefined;
};

/** @internal */
export const CaptureParams$outboundSchema: z.ZodType<
  CaptureParams$Outbound,
  z.ZodTypeDef,
  CaptureParams
> = z.object({
  duration: z.number(),
  filter: z.string(),
  level: CaptureLevel$outboundSchema,
  maxEvents: z.number().int(),
  stepDuration: z.number().int().optional(),
  workerId: z.string().optional(),
  workerThreshold: z.number().int().optional(),
});

export function captureParamsToJSON(captureParams: CaptureParams): string {
  return JSON.stringify(CaptureParams$outboundSchema.parse(captureParams));
}
export function captureParamsFromJSON(
  jsonString: string,
): SafeParseResult<CaptureParams, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CaptureParams$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CaptureParams' from JSON`,
  );
}
