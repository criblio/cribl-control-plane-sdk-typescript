/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type FunctionConfSchemaEventstats = {
  /**
   * Aggregate function(s) to perform on events. E.g., sum(bytes).where(action=='REJECT').as(TotalBytes)
   */
  aggregations?: Array<string> | undefined;
  /**
   * Fields to group aggregates by, supports wildcard expressions.
   */
  groupBys?: Array<string> | undefined;
  /**
   * Specifies how many events are at max kept in memory to be enriched with aggregations
   */
  maxEvents: number;
  /**
   * Determines if aggregations should flush when an input stream is closed. If disabled, time window settings will control flush behavior.
   */
  flushOnInputClose: boolean;
};

/** @internal */
export const FunctionConfSchemaEventstats$inboundSchema: z.ZodType<
  FunctionConfSchemaEventstats,
  z.ZodTypeDef,
  unknown
> = z.object({
  aggregations: z.array(z.string()).optional(),
  groupBys: z.array(z.string()).optional(),
  maxEvents: z.number().default(50000),
  flushOnInputClose: z.boolean().default(false),
});

export function functionConfSchemaEventstatsFromJSON(
  jsonString: string,
): SafeParseResult<FunctionConfSchemaEventstats, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FunctionConfSchemaEventstats$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FunctionConfSchemaEventstats' from JSON`,
  );
}
