/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type PackUninstallInfo = {
  id: string;
  source: string;
};

/** @internal */
export const PackUninstallInfo$inboundSchema: z.ZodType<
  PackUninstallInfo,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: types.string(),
  source: types.string(),
});
/** @internal */
export type PackUninstallInfo$Outbound = {
  id: string;
  source: string;
};

/** @internal */
export const PackUninstallInfo$outboundSchema: z.ZodType<
  PackUninstallInfo$Outbound,
  z.ZodTypeDef,
  PackUninstallInfo
> = z.object({
  id: z.string(),
  source: z.string(),
});

export function packUninstallInfoToJSON(
  packUninstallInfo: PackUninstallInfo,
): string {
  return JSON.stringify(
    PackUninstallInfo$outboundSchema.parse(packUninstallInfo),
  );
}
export function packUninstallInfoFromJSON(
  jsonString: string,
): SafeParseResult<PackUninstallInfo, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PackUninstallInfo$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PackUninstallInfo' from JSON`,
  );
}
