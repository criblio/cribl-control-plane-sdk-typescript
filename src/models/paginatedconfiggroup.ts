/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { ConfigGroup, ConfigGroup$inboundSchema } from "./configgroup.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type PaginatedConfigGroup = {
  /**
   * the pre-limited items in the list of results
   */
  items: Array<ConfigGroup>;
  /**
   * number of items present in the items array
   */
  count: number;
  /**
   * pagination offset
   */
  offset: number;
  /**
   * pagination limit
   */
  limit: number;
  /**
   * total number of items available (present when limit is set)
   */
  totalCount?: number | undefined;
};

/** @internal */
export const PaginatedConfigGroup$inboundSchema: z.ZodType<
  PaginatedConfigGroup,
  z.ZodTypeDef,
  unknown
> = z.object({
  items: z.array(ConfigGroup$inboundSchema),
  count: types.number(),
  offset: types.number(),
  limit: types.number(),
  totalCount: types.optional(types.number()),
});

export function paginatedConfigGroupFromJSON(
  jsonString: string,
): SafeParseResult<PaginatedConfigGroup, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PaginatedConfigGroup$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PaginatedConfigGroup' from JSON`,
  );
}
