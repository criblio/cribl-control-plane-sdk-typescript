/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { OutputStatus, OutputStatus$inboundSchema } from "./outputstatus.js";

export type PaginatedOutputStatus = {
  /**
   * The pre-limited items in the list of results
   */
  items: Array<OutputStatus>;
  /**
   * Number of items present in the items array
   */
  count: number;
  /**
   * Pagination offset
   */
  offset?: number | undefined;
  /**
   * Pagination limit
   */
  limit?: number | undefined;
  /**
   * Total number of items available (present when limit is set)
   */
  totalCount?: number | undefined;
};

/** @internal */
export const PaginatedOutputStatus$inboundSchema: z.ZodType<
  PaginatedOutputStatus,
  z.ZodTypeDef,
  unknown
> = z.object({
  items: z.array(OutputStatus$inboundSchema),
  count: types.number(),
  offset: types.optional(types.number()),
  limit: types.optional(types.number()),
  totalCount: types.optional(types.number()),
});

export function paginatedOutputStatusFromJSON(
  jsonString: string,
): SafeParseResult<PaginatedOutputStatus, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PaginatedOutputStatus$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PaginatedOutputStatus' from JSON`,
  );
}
