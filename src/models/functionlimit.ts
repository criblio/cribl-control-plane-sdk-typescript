/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type FunctionLimitConf = {
  /**
   * Number of qualifying events to pass through
   */
  limit?: number | undefined;
};

export const FunctionLimitId = {
  Limit: "limit",
} as const;
export type FunctionLimitId = ClosedEnum<typeof FunctionLimitId>;

export type FunctionLimit = {
  conf: FunctionLimitConf;
  filename: string;
  disabled: boolean;
  group: string;
  id: FunctionLimitId;
  initTime: number;
  loadTime: number;
  modTime: number;
  name: string;
  /**
   * JSON Schema Draft-06
   */
  schema: { [k: string]: any };
  uischema: { [k: string]: any };
  version: string;
};

/** @internal */
export const FunctionLimitConf$inboundSchema: z.ZodType<
  FunctionLimitConf,
  z.ZodTypeDef,
  unknown
> = z.object({
  limit: z.number().int().default(100),
});

export function functionLimitConfFromJSON(
  jsonString: string,
): SafeParseResult<FunctionLimitConf, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FunctionLimitConf$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FunctionLimitConf' from JSON`,
  );
}

/** @internal */
export const FunctionLimitId$inboundSchema: z.ZodNativeEnum<
  typeof FunctionLimitId
> = z.nativeEnum(FunctionLimitId);

/** @internal */
export const FunctionLimit$inboundSchema: z.ZodType<
  FunctionLimit,
  z.ZodTypeDef,
  unknown
> = z.object({
  __conf: z.lazy(() => FunctionLimitConf$inboundSchema),
  __filename: z.string(),
  disabled: z.boolean(),
  group: z.string(),
  id: FunctionLimitId$inboundSchema,
  initTime: z.number(),
  loadTime: z.number(),
  modTime: z.number(),
  name: z.string(),
  schema: z.record(z.any()),
  uischema: z.record(z.any()),
  version: z.string(),
}).transform((v) => {
  return remap$(v, {
    "__conf": "conf",
    "__filename": "filename",
  });
});

export function functionLimitFromJSON(
  jsonString: string,
): SafeParseResult<FunctionLimit, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FunctionLimit$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FunctionLimit' from JSON`,
  );
}
