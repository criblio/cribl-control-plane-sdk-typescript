/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type Owner = {
  kind: string;
  name: string;
};

export type KubeType = {
  enabled: boolean;
  namespace: string;
  node: string;
  owner?: Owner | undefined;
  pod: string;
  source: string;
};

/** @internal */
export const Owner$inboundSchema: z.ZodType<Owner, z.ZodTypeDef, unknown> = z
  .object({
    kind: z.string(),
    name: z.string(),
  });

export function ownerFromJSON(
  jsonString: string,
): SafeParseResult<Owner, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Owner$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Owner' from JSON`,
  );
}

/** @internal */
export const KubeType$inboundSchema: z.ZodType<
  KubeType,
  z.ZodTypeDef,
  unknown
> = z.object({
  enabled: z.boolean(),
  namespace: z.string(),
  node: z.string(),
  owner: z.lazy(() => Owner$inboundSchema).optional(),
  pod: z.string(),
  source: z.string(),
});

export function kubeTypeFromJSON(
  jsonString: string,
): SafeParseResult<KubeType, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => KubeType$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'KubeType' from JSON`,
  );
}
