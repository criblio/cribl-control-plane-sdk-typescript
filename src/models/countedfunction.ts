/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { FunctionT, FunctionT$inboundSchema } from "./function.js";

export type CountedFunction = {
  /**
   * number of items present in the items array
   */
  count?: number | undefined;
  items?: Array<FunctionT> | undefined;
};

/** @internal */
export const CountedFunction$inboundSchema: z.ZodType<
  CountedFunction,
  z.ZodTypeDef,
  unknown
> = z.object({
  count: z.number().int().optional(),
  items: z.array(FunctionT$inboundSchema).optional(),
});

export function countedFunctionFromJSON(
  jsonString: string,
): SafeParseResult<CountedFunction, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CountedFunction$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CountedFunction' from JSON`,
  );
}
