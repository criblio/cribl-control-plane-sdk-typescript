/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { TaskErrorInfo, TaskErrorInfo$inboundSchema } from "./taskerrorinfo.js";

/**
 * Task error details. May include a nested <code>reason</code> for wrapped errors and additional properties from the original error.
 */
export type TaskErrorDetail = {
  /**
   * Human-readable error message.
   */
  message: string;
  /**
   * Error name, if available.
   */
  name?: string | undefined;
  /**
   * Serialized error object that describes why a job entered its current <code>state</code>. Includes <code>message</code> and may include a nested <code>reason</code> for wrapped errors.
   */
  reason?: TaskErrorInfo | undefined;
  /**
   * Truncated stack trace of the error.
   */
  stack?: string | undefined;
};

/** @internal */
export const TaskErrorDetail$inboundSchema: z.ZodType<
  TaskErrorDetail,
  z.ZodTypeDef,
  unknown
> = z.object({
  message: types.string(),
  name: types.optional(types.string()),
  reason: types.optional(TaskErrorInfo$inboundSchema),
  stack: types.optional(types.string()),
});

export function taskErrorDetailFromJSON(
  jsonString: string,
): SafeParseResult<TaskErrorDetail, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TaskErrorDetail$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TaskErrorDetail' from JSON`,
  );
}
