/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { ClosedEnum, OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Defines how sample rate will be derived: log(previousPeriodCount) or sqrt(previousPeriodCount)
 */
export const SampleMode = {
  Log: "log",
  Sqrt: "sqrt",
} as const;
/**
 * Defines how sample rate will be derived: log(previousPeriodCount) or sqrt(previousPeriodCount)
 */
export type SampleMode = OpenEnum<typeof SampleMode>;

export type FunctionDynamicSamplingConf = {
  /**
   * Defines how sample rate will be derived: log(previousPeriodCount) or sqrt(previousPeriodCount)
   */
  mode?: SampleMode | undefined;
  /**
   * Expression used to derive sample group key. Example:`${domain}:${status}`. Each sample group will have its own derived sampling rate based on volume. Defaults to `${host}`.
   */
  keyExpr?: string | undefined;
  /**
   * How often (in seconds) sample rates will be adjusted
   */
  samplePeriod?: number | undefined;
  /**
   * Minimum number of events that must be received in previous sample period for sampling mode to be applied to current period. If the number of events received for a sample group is less than this minimum, a sample rate of 1:1 is used.
   */
  minEvents?: number | undefined;
  /**
   * Maximum sampling rate. If computed sampling rate is above this value, it will be limited to this value.
   */
  maxSampleRate?: number | undefined;
};

export const FunctionDynamicSamplingId = {
  DynamicSampling: "dynamic_sampling",
} as const;
export type FunctionDynamicSamplingId = ClosedEnum<
  typeof FunctionDynamicSamplingId
>;

export type FunctionDynamicSampling = {
  conf: FunctionDynamicSamplingConf;
  filename: string;
  disabled: boolean;
  group: string;
  id: FunctionDynamicSamplingId;
  initTime: number;
  loadTime: number;
  modTime: number;
  name: string;
  /**
   * JSON Schema Draft-06
   */
  schema: { [k: string]: any };
  uischema: { [k: string]: any };
  version: string;
};

/** @internal */
export const SampleMode$inboundSchema: z.ZodType<
  SampleMode,
  z.ZodTypeDef,
  unknown
> = openEnums.inboundSchema(SampleMode);

/** @internal */
export const FunctionDynamicSamplingConf$inboundSchema: z.ZodType<
  FunctionDynamicSamplingConf,
  z.ZodTypeDef,
  unknown
> = z.object({
  mode: SampleMode$inboundSchema.default("log"),
  keyExpr: z.string().default("`${host}`"),
  samplePeriod: z.number().default(30),
  minEvents: z.number().default(30),
  maxSampleRate: z.number().default(100),
});

export function functionDynamicSamplingConfFromJSON(
  jsonString: string,
): SafeParseResult<FunctionDynamicSamplingConf, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FunctionDynamicSamplingConf$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FunctionDynamicSamplingConf' from JSON`,
  );
}

/** @internal */
export const FunctionDynamicSamplingId$inboundSchema: z.ZodNativeEnum<
  typeof FunctionDynamicSamplingId
> = z.nativeEnum(FunctionDynamicSamplingId);

/** @internal */
export const FunctionDynamicSampling$inboundSchema: z.ZodType<
  FunctionDynamicSampling,
  z.ZodTypeDef,
  unknown
> = z.object({
  __conf: z.lazy(() => FunctionDynamicSamplingConf$inboundSchema),
  __filename: z.string(),
  disabled: z.boolean(),
  group: z.string(),
  id: FunctionDynamicSamplingId$inboundSchema,
  initTime: z.number(),
  loadTime: z.number(),
  modTime: z.number(),
  name: z.string(),
  schema: z.record(z.any()),
  uischema: z.record(z.any()),
  version: z.string(),
}).transform((v) => {
  return remap$(v, {
    "__conf": "conf",
    "__filename": "filename",
  });
});

export function functionDynamicSamplingFromJSON(
  jsonString: string,
): SafeParseResult<FunctionDynamicSampling, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FunctionDynamicSampling$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FunctionDynamicSampling' from JSON`,
  );
}
