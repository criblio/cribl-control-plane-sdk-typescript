/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * In Sender mode, forwards search results directly to the destination. In Metrics mode, accumulates metrics from federated send operators, and forwards the aggregate metrics.
 */
export const FunctionConfSchemaSendMode = {
  Sender: "sender",
  Metrics: "metrics",
} as const;
/**
 * In Sender mode, forwards search results directly to the destination. In Metrics mode, accumulates metrics from federated send operators, and forwards the aggregate metrics.
 */
export type FunctionConfSchemaSendMode = OpenEnum<
  typeof FunctionConfSchemaSendMode
>;

export type FunctionConfSchemaSend = {
  /**
   * Full URL to send search to.
   */
  url?: string | undefined;
  /**
   * Group within the workspace we're sending to.
   */
  group?: string | undefined;
  /**
   * Workspace within the deployment to send the search results to.
   */
  workspace?: string | undefined;
  /**
   * Template to build the URL to send from.
   */
  sendUrlTemplate?: string | undefined;
  /**
   * Id of the search this function is running on.
   */
  searchId?: string | undefined;
  /**
   * Tee results to search. When set to true results will be shipped instead of stats
   */
  tee?: boolean | undefined;
  /**
   * How often are stats flushed in ms
   */
  flushMs?: number | undefined;
  /**
   * Disables generation of intermediate stats. When true stats will be emitted only on end
   */
  suppressPreviews?: boolean | undefined;
  /**
   * In Sender mode, forwards search results directly to the destination. In Metrics mode, accumulates metrics from federated send operators, and forwards the aggregate metrics.
   */
  mode?: FunctionConfSchemaSendMode | undefined;
};

/** @internal */
export const FunctionConfSchemaSendMode$inboundSchema: z.ZodType<
  FunctionConfSchemaSendMode,
  z.ZodTypeDef,
  unknown
> = openEnums.inboundSchema(FunctionConfSchemaSendMode);

/** @internal */
export const FunctionConfSchemaSend$inboundSchema: z.ZodType<
  FunctionConfSchemaSend,
  z.ZodTypeDef,
  unknown
> = z.object({
  url: z.string().optional(),
  group: z.string().optional(),
  workspace: z.string().optional(),
  sendUrlTemplate: z.string().optional(),
  searchId: z.string().optional(),
  tee: z.boolean().optional(),
  flushMs: z.number().optional(),
  suppressPreviews: z.boolean().optional(),
  mode: FunctionConfSchemaSendMode$inboundSchema.optional(),
});

export function functionConfSchemaSendFromJSON(
  jsonString: string,
): SafeParseResult<FunctionConfSchemaSend, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FunctionConfSchemaSend$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FunctionConfSchemaSend' from JSON`,
  );
}
