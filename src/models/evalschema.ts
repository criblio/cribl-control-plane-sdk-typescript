/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type Add = {
  name?: string | undefined;
  /**
   * JavaScript expression to compute the value (can be constant)
   */
  value: string;
  /**
   * Set to No to disable the evaluation of an individual expression
   */
  disabled?: boolean | undefined;
};

export type EvalSchema = {
  /**
   * Set of key-value pairs to evaluate and add/set
   */
  add?: Array<Add> | undefined;
  /**
   * List of fields to keep. Supports * wildcards. Takes precedence over 'Remove fields'.
   */
  keep?: Array<string> | undefined;
  /**
   * List of fields to remove. Supports * wildcards. Fields that match 'Keep fields' will not be removed. Enclose field names containing special characters in single or double quotes.
   */
  remove?: Array<string> | undefined;
};

/** @internal */
export const Add$inboundSchema: z.ZodType<Add, z.ZodTypeDef, unknown> = z
  .object({
    name: z.string().optional(),
    value: z.string(),
    disabled: z.boolean().default(false),
  });

/** @internal */
export type Add$Outbound = {
  name?: string | undefined;
  value: string;
  disabled: boolean;
};

/** @internal */
export const Add$outboundSchema: z.ZodType<Add$Outbound, z.ZodTypeDef, Add> = z
  .object({
    name: z.string().optional(),
    value: z.string(),
    disabled: z.boolean().default(false),
  });

/**
 * @internal
 * @deprecated This namespace will be removed in future versions. Use schemas and types that are exported directly from this module.
 */
export namespace Add$ {
  /** @deprecated use `Add$inboundSchema` instead. */
  export const inboundSchema = Add$inboundSchema;
  /** @deprecated use `Add$outboundSchema` instead. */
  export const outboundSchema = Add$outboundSchema;
  /** @deprecated use `Add$Outbound` instead. */
  export type Outbound = Add$Outbound;
}

export function addToJSON(add: Add): string {
  return JSON.stringify(Add$outboundSchema.parse(add));
}

export function addFromJSON(
  jsonString: string,
): SafeParseResult<Add, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Add$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Add' from JSON`,
  );
}

/** @internal */
export const EvalSchema$inboundSchema: z.ZodType<
  EvalSchema,
  z.ZodTypeDef,
  unknown
> = z.object({
  add: z.array(z.lazy(() => Add$inboundSchema)).optional(),
  keep: z.array(z.string()).optional(),
  remove: z.array(z.string()).optional(),
});

/** @internal */
export type EvalSchema$Outbound = {
  add?: Array<Add$Outbound> | undefined;
  keep?: Array<string> | undefined;
  remove?: Array<string> | undefined;
};

/** @internal */
export const EvalSchema$outboundSchema: z.ZodType<
  EvalSchema$Outbound,
  z.ZodTypeDef,
  EvalSchema
> = z.object({
  add: z.array(z.lazy(() => Add$outboundSchema)).optional(),
  keep: z.array(z.string()).optional(),
  remove: z.array(z.string()).optional(),
});

/**
 * @internal
 * @deprecated This namespace will be removed in future versions. Use schemas and types that are exported directly from this module.
 */
export namespace EvalSchema$ {
  /** @deprecated use `EvalSchema$inboundSchema` instead. */
  export const inboundSchema = EvalSchema$inboundSchema;
  /** @deprecated use `EvalSchema$outboundSchema` instead. */
  export const outboundSchema = EvalSchema$outboundSchema;
  /** @deprecated use `EvalSchema$Outbound` instead. */
  export type Outbound = EvalSchema$Outbound;
}

export function evalSchemaToJSON(evalSchema: EvalSchema): string {
  return JSON.stringify(EvalSchema$outboundSchema.parse(evalSchema));
}

export function evalSchemaFromJSON(
  jsonString: string,
): SafeParseResult<EvalSchema, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EvalSchema$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EvalSchema' from JSON`,
  );
}
