/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type ActiveHealthOverlayStatus = {
  /**
   * Binary version targeted by the active overlay.
   */
  binaryVersion: string;
  /**
   * Active overlay identifier.
   */
  overlayId: string;
  /**
   * Current overlay state.
   */
  state: "active";
};

/** @internal */
export const ActiveHealthOverlayStatus$inboundSchema: z.ZodType<
  ActiveHealthOverlayStatus,
  z.ZodTypeDef,
  unknown
> = z.object({
  binaryVersion: types.string(),
  overlayId: types.string(),
  state: types.literal("active"),
});

export function activeHealthOverlayStatusFromJSON(
  jsonString: string,
): SafeParseResult<ActiveHealthOverlayStatus, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ActiveHealthOverlayStatus$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ActiveHealthOverlayStatus' from JSON`,
  );
}
