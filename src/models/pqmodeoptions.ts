/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import {
  catchUnrecognizedEnum,
  OpenEnum,
  Unrecognized,
} from "../types/enums.js";

/**
 * In Error mode, PQ writes events to the filesystem if the Destination is unavailable. In Backpressure mode, PQ writes events to the filesystem when it detects backpressure from the Destination. In Always On mode, PQ always writes events to the filesystem.
 */
export const PqModeOptions = {
  /**
   * Error
   */
  Error: "error",
  /**
   * Backpressure
   */
  Always: "always",
  /**
   * Always On
   */
  Backpressure: "backpressure",
} as const;
/**
 * In Error mode, PQ writes events to the filesystem if the Destination is unavailable. In Backpressure mode, PQ writes events to the filesystem when it detects backpressure from the Destination. In Always On mode, PQ always writes events to the filesystem.
 */
export type PqModeOptions = OpenEnum<typeof PqModeOptions>;

/** @internal */
export const PqModeOptions$inboundSchema: z.ZodType<
  PqModeOptions,
  z.ZodTypeDef,
  unknown
> = z
  .union([
    z.nativeEnum(PqModeOptions),
    z.string().transform(catchUnrecognizedEnum),
  ]);
/** @internal */
export const PqModeOptions$outboundSchema: z.ZodType<
  PqModeOptions,
  z.ZodTypeDef,
  PqModeOptions
> = z.union([
  z.nativeEnum(PqModeOptions),
  z.string().and(z.custom<Unrecognized<string>>()),
]);
