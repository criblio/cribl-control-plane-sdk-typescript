/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type GitFileRename = {
  /**
   * Original file path before the rename.
   */
  from: string;
  /**
   * New file path after the rename.
   */
  to: string;
};

/** @internal */
export const GitFileRename$inboundSchema: z.ZodType<
  GitFileRename,
  z.ZodTypeDef,
  unknown
> = z.object({
  from: types.string(),
  to: types.string(),
});

export function gitFileRenameFromJSON(
  jsonString: string,
): SafeParseResult<GitFileRename, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GitFileRename$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GitFileRename' from JSON`,
  );
}
