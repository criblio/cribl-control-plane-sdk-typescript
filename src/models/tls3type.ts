/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type Tls3Type = {
  disabled?: boolean | undefined;
  /**
   * Reject certificates that are not authorized by a CA in the CA certificate path, or by another trusted CA (such as the system's)
   */
  rejectUnauthorized?: boolean | undefined;
};

/** @internal */
export const Tls3Type$inboundSchema: z.ZodType<
  Tls3Type,
  z.ZodTypeDef,
  unknown
> = z.object({
  disabled: z.boolean().default(false),
  rejectUnauthorized: z.boolean().default(true),
});
/** @internal */
export type Tls3Type$Outbound = {
  disabled: boolean;
  rejectUnauthorized: boolean;
};

/** @internal */
export const Tls3Type$outboundSchema: z.ZodType<
  Tls3Type$Outbound,
  z.ZodTypeDef,
  Tls3Type
> = z.object({
  disabled: z.boolean().default(false),
  rejectUnauthorized: z.boolean().default(true),
});

export function tls3TypeToJSON(tls3Type: Tls3Type): string {
  return JSON.stringify(Tls3Type$outboundSchema.parse(tls3Type));
}
export function tls3TypeFromJSON(
  jsonString: string,
): SafeParseResult<Tls3Type, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Tls3Type$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Tls3Type' from JSON`,
  );
}
