/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type MetadataItem = {
  /**
   * Field name. Must be a valid identifier (letter, underscore, or $ followed by alphanumeric, or quoted). Surround with quotes or backticks if the name contains special characters.
   */
  name: string;
  /**
   * JavaScript expression to compute the field value, enclosed in quotes or backticks. Can evaluate to a constant.
   */
  value: string;
};

/** @internal */
export const MetadataItem$inboundSchema: z.ZodType<
  MetadataItem,
  z.ZodTypeDef,
  unknown
> = z.object({
  name: types.string(),
  value: types.string(),
});

export function metadataItemFromJSON(
  jsonString: string,
): SafeParseResult<MetadataItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MetadataItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MetadataItem' from JSON`,
  );
}
