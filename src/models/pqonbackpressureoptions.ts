/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import {
  catchUnrecognizedEnum,
  OpenEnum,
  Unrecognized,
} from "../types/enums.js";

/**
 * How to handle events when the queue is exerting backpressure (full capacity or low disk). 'Block' is the same behavior as non-PQ blocking. 'Drop new data' throws away incoming data, while leaving the contents of the PQ unchanged.
 */
export const PqOnBackpressureOptions = {
  /**
   * Block
   */
  Block: "block",
  /**
   * Drop new data
   */
  Drop: "drop",
} as const;
/**
 * How to handle events when the queue is exerting backpressure (full capacity or low disk). 'Block' is the same behavior as non-PQ blocking. 'Drop new data' throws away incoming data, while leaving the contents of the PQ unchanged.
 */
export type PqOnBackpressureOptions = OpenEnum<typeof PqOnBackpressureOptions>;

/** @internal */
export const PqOnBackpressureOptions$inboundSchema: z.ZodType<
  PqOnBackpressureOptions,
  z.ZodTypeDef,
  unknown
> = z
  .union([
    z.nativeEnum(PqOnBackpressureOptions),
    z.string().transform(catchUnrecognizedEnum),
  ]);
/** @internal */
export const PqOnBackpressureOptions$outboundSchema: z.ZodType<
  PqOnBackpressureOptions,
  z.ZodTypeDef,
  PqOnBackpressureOptions
> = z.union([
  z.nativeEnum(PqOnBackpressureOptions),
  z.string().and(z.custom<Unrecognized<string>>()),
]);
