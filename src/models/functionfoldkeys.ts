/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type FunctionFoldkeysConf = {
  /**
   * When enabled (default), only the folded keys are kept. When disabled, the original entries are retained alongside the folded keys.
   */
  deleteOriginal?: boolean | undefined;
  /**
   * Character or string used to separate key levels to be folded. Defaults to the dot (.) character.
   */
  separator?: string | undefined;
  /**
   * Optional regular expression to select a subset of the keys to fold.
   */
  selectionRegExp?: string | undefined;
};

export const FunctionFoldkeysId = {
  Foldkeys: "foldkeys",
} as const;
export type FunctionFoldkeysId = ClosedEnum<typeof FunctionFoldkeysId>;

export type FunctionFoldkeys = {
  conf: FunctionFoldkeysConf;
  filename: string;
  disabled: boolean;
  group: string;
  id: FunctionFoldkeysId;
  initTime: number;
  loadTime: number;
  modTime: number;
  name: string;
  /**
   * JSON Schema Draft-06
   */
  schema: { [k: string]: any };
  uischema: { [k: string]: any };
  version: string;
};

/** @internal */
export const FunctionFoldkeysConf$inboundSchema: z.ZodType<
  FunctionFoldkeysConf,
  z.ZodTypeDef,
  unknown
> = z.object({
  deleteOriginal: z.boolean().default(true),
  separator: z.string().default("."),
  selectionRegExp: z.string().optional(),
});

export function functionFoldkeysConfFromJSON(
  jsonString: string,
): SafeParseResult<FunctionFoldkeysConf, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FunctionFoldkeysConf$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FunctionFoldkeysConf' from JSON`,
  );
}

/** @internal */
export const FunctionFoldkeysId$inboundSchema: z.ZodNativeEnum<
  typeof FunctionFoldkeysId
> = z.nativeEnum(FunctionFoldkeysId);

/** @internal */
export const FunctionFoldkeys$inboundSchema: z.ZodType<
  FunctionFoldkeys,
  z.ZodTypeDef,
  unknown
> = z.object({
  __conf: z.lazy(() => FunctionFoldkeysConf$inboundSchema),
  __filename: z.string(),
  disabled: z.boolean(),
  group: z.string(),
  id: FunctionFoldkeysId$inboundSchema,
  initTime: z.number(),
  loadTime: z.number(),
  modTime: z.number(),
  name: z.string(),
  schema: z.record(z.any()),
  uischema: z.record(z.any()),
  version: z.string(),
}).transform((v) => {
  return remap$(v, {
    "__conf": "conf",
    "__filename": "filename",
  });
});

export function functionFoldkeysFromJSON(
  jsonString: string,
): SafeParseResult<FunctionFoldkeys, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FunctionFoldkeys$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FunctionFoldkeys' from JSON`,
  );
}
