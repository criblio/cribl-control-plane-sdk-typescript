/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import {
  catchUnrecognizedEnum,
  OpenEnum,
  Unrecognized,
} from "../types/enums.js";

/**
 * The network protocol to use for sending out syslog messages
 */
export const Protocol1Options = {
  /**
   * TCP
   */
  Tcp: "tcp",
  /**
   * UDP
   */
  Udp: "udp",
} as const;
/**
 * The network protocol to use for sending out syslog messages
 */
export type Protocol1Options = OpenEnum<typeof Protocol1Options>;

/** @internal */
export const Protocol1Options$inboundSchema: z.ZodType<
  Protocol1Options,
  z.ZodTypeDef,
  unknown
> = z
  .union([
    z.nativeEnum(Protocol1Options),
    z.string().transform(catchUnrecognizedEnum),
  ]);
/** @internal */
export const Protocol1Options$outboundSchema: z.ZodType<
  Protocol1Options,
  z.ZodTypeDef,
  Protocol1Options
> = z.union([
  z.nativeEnum(Protocol1Options),
  z.string().and(z.custom<Unrecognized<string>>()),
]);
