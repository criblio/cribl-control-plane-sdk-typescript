/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type RestartRequest = {
  /**
   * GUIDs of the Worker or Edge Nodes to restart.
   */
  guids: Array<string>;
};

/** @internal */
export const RestartRequest$inboundSchema: z.ZodType<
  RestartRequest,
  z.ZodTypeDef,
  unknown
> = z.object({
  guids: z.array(types.string()),
});
/** @internal */
export type RestartRequest$Outbound = {
  guids: Array<string>;
};

/** @internal */
export const RestartRequest$outboundSchema: z.ZodType<
  RestartRequest$Outbound,
  z.ZodTypeDef,
  RestartRequest
> = z.object({
  guids: z.array(z.string()),
});

export function restartRequestToJSON(restartRequest: RestartRequest): string {
  return JSON.stringify(RestartRequest$outboundSchema.parse(restartRequest));
}
export function restartRequestFromJSON(
  jsonString: string,
): SafeParseResult<RestartRequest, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RestartRequest$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RestartRequest' from JSON`,
  );
}
