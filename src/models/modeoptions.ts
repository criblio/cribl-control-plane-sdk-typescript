/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";

/**
 * In Error mode, PQ writes events to the filesystem if the Destination is unavailable. In Backpressure mode, PQ writes events to the filesystem when it detects backpressure from the Destination. In Always On mode, PQ always writes events to the filesystem.
 */
export const ModeOptions = {
  /**
   * Error
   */
  Error: "error",
  /**
   * Backpressure
   */
  Always: "always",
  /**
   * Always On
   */
  Backpressure: "backpressure",
} as const;
/**
 * In Error mode, PQ writes events to the filesystem if the Destination is unavailable. In Backpressure mode, PQ writes events to the filesystem when it detects backpressure from the Destination. In Always On mode, PQ always writes events to the filesystem.
 */
export type ModeOptions = OpenEnum<typeof ModeOptions>;

/** @internal */
export const ModeOptions$inboundSchema: z.ZodType<
  ModeOptions,
  z.ZodTypeDef,
  unknown
> = openEnums.inboundSchema(ModeOptions);
/** @internal */
export const ModeOptions$outboundSchema: z.ZodType<
  string,
  z.ZodTypeDef,
  ModeOptions
> = openEnums.outboundSchema(ModeOptions);
