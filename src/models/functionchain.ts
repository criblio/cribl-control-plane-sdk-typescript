/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type FunctionChainConf = {
  /**
   * The data processor (Pack/Pipeline) to send events through
   */
  processor: string;
};

export const FunctionChainId = {
  Chain: "chain",
} as const;
export type FunctionChainId = ClosedEnum<typeof FunctionChainId>;

export type FunctionChain = {
  conf: FunctionChainConf;
  filename: string;
  disabled: boolean;
  group: string;
  id: FunctionChainId;
  initTime: number;
  loadTime: number;
  modTime: number;
  name: string;
  /**
   * JSON Schema Draft-06
   */
  schema: { [k: string]: any };
  uischema: { [k: string]: any };
  version: string;
};

/** @internal */
export const FunctionChainConf$inboundSchema: z.ZodType<
  FunctionChainConf,
  z.ZodTypeDef,
  unknown
> = z.object({
  processor: z.string(),
});

export function functionChainConfFromJSON(
  jsonString: string,
): SafeParseResult<FunctionChainConf, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FunctionChainConf$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FunctionChainConf' from JSON`,
  );
}

/** @internal */
export const FunctionChainId$inboundSchema: z.ZodNativeEnum<
  typeof FunctionChainId
> = z.nativeEnum(FunctionChainId);

/** @internal */
export const FunctionChain$inboundSchema: z.ZodType<
  FunctionChain,
  z.ZodTypeDef,
  unknown
> = z.object({
  __conf: z.lazy(() => FunctionChainConf$inboundSchema),
  __filename: z.string(),
  disabled: z.boolean(),
  group: z.string(),
  id: FunctionChainId$inboundSchema,
  initTime: z.number(),
  loadTime: z.number(),
  modTime: z.number(),
  name: z.string(),
  schema: z.record(z.any()),
  uischema: z.record(z.any()),
  version: z.string(),
}).transform((v) => {
  return remap$(v, {
    "__conf": "conf",
    "__filename": "filename",
  });
});

export function functionChainFromJSON(
  jsonString: string,
): SafeParseResult<FunctionChain, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FunctionChain$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FunctionChain' from JSON`,
  );
}
