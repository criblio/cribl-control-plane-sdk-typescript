/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export const FunctionChainId = {
  Chain: "chain",
} as const;
export type FunctionChainId = ClosedEnum<typeof FunctionChainId>;

export type FunctionChainSchema = {
  /**
   * The data processor (Pack/Pipeline) to send events through
   */
  processor: string;
};

export type FunctionChain = {
  filename: string;
  asyncTimeout?: number | undefined;
  criblVersion?: string | undefined;
  disabled: boolean;
  group: string;
  handleSignals?: boolean | undefined;
  id: FunctionChainId;
  loadTime: number;
  modTime: number;
  name: string;
  sync?: boolean | undefined;
  uischema: { [k: string]: any };
  version: string;
  schema?: FunctionChainSchema | undefined;
};

/** @internal */
export const FunctionChainId$inboundSchema: z.ZodNativeEnum<
  typeof FunctionChainId
> = z.nativeEnum(FunctionChainId);

/** @internal */
export const FunctionChainSchema$inboundSchema: z.ZodType<
  FunctionChainSchema,
  z.ZodTypeDef,
  unknown
> = z.object({
  processor: z.string(),
});

export function functionChainSchemaFromJSON(
  jsonString: string,
): SafeParseResult<FunctionChainSchema, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FunctionChainSchema$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FunctionChainSchema' from JSON`,
  );
}

/** @internal */
export const FunctionChain$inboundSchema: z.ZodType<
  FunctionChain,
  z.ZodTypeDef,
  unknown
> = z.object({
  __filename: z.string(),
  asyncTimeout: z.number().optional(),
  cribl_version: z.string().optional(),
  disabled: z.boolean(),
  group: z.string(),
  handleSignals: z.boolean().optional(),
  id: FunctionChainId$inboundSchema,
  loadTime: z.number(),
  modTime: z.number(),
  name: z.string(),
  sync: z.boolean().optional(),
  uischema: z.record(z.any()),
  version: z.string(),
  schema: z.lazy(() => FunctionChainSchema$inboundSchema).optional(),
}).transform((v) => {
  return remap$(v, {
    "__filename": "filename",
    "cribl_version": "criblVersion",
  });
});

export function functionChainFromJSON(
  jsonString: string,
): SafeParseResult<FunctionChain, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FunctionChain$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FunctionChain' from JSON`,
  );
}
