/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type FunctionWindowConf = {
  /**
   * Identifies the unique ID, used for a event window
   */
  eventWindowId: number;
  /**
   * All window functions, tracked by this event window
   */
  registeredFunctions: Array<string>;
  /**
   * Number of events to keep before the current event in the window
   */
  tailEventCount?: number | undefined;
  /**
   * Number of events to keep after the current event in the window
   */
  headEventCount?: number | undefined;
};

export const FunctionWindowId = {
  Window: "window",
} as const;
export type FunctionWindowId = ClosedEnum<typeof FunctionWindowId>;

export type FunctionWindow = {
  conf: FunctionWindowConf;
  filename: string;
  disabled: boolean;
  group: string;
  id: FunctionWindowId;
  initTime: number;
  loadTime: number;
  modTime: number;
  name: string;
  /**
   * JSON Schema Draft-06
   */
  schema: { [k: string]: any };
  uischema: { [k: string]: any };
  version: string;
};

/** @internal */
export const FunctionWindowConf$inboundSchema: z.ZodType<
  FunctionWindowConf,
  z.ZodTypeDef,
  unknown
> = z.object({
  eventWindowId: z.number(),
  registeredFunctions: z.array(z.string()),
  tailEventCount: z.number().default(0),
  headEventCount: z.number().default(0),
});

export function functionWindowConfFromJSON(
  jsonString: string,
): SafeParseResult<FunctionWindowConf, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FunctionWindowConf$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FunctionWindowConf' from JSON`,
  );
}

/** @internal */
export const FunctionWindowId$inboundSchema: z.ZodNativeEnum<
  typeof FunctionWindowId
> = z.nativeEnum(FunctionWindowId);

/** @internal */
export const FunctionWindow$inboundSchema: z.ZodType<
  FunctionWindow,
  z.ZodTypeDef,
  unknown
> = z.object({
  __conf: z.lazy(() => FunctionWindowConf$inboundSchema),
  __filename: z.string(),
  disabled: z.boolean(),
  group: z.string(),
  id: FunctionWindowId$inboundSchema,
  initTime: z.number(),
  loadTime: z.number(),
  modTime: z.number(),
  name: z.string(),
  schema: z.record(z.any()),
  uischema: z.record(z.any()),
  version: z.string(),
}).transform((v) => {
  return remap$(v, {
    "__conf": "conf",
    "__filename": "filename",
  });
});

export function functionWindowFromJSON(
  jsonString: string,
): SafeParseResult<FunctionWindow, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FunctionWindow$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FunctionWindow' from JSON`,
  );
}
