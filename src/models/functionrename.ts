/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type Rename = {
  /**
   * Name of the field to rename. Literal identifiers must be quoted.
   */
  currentName: string;
  /**
   * The name the field will be renamed to. Literal identifiers must be quoted.
   */
  newName: string;
};

export type FunctionRenameConf = {
  /**
   * Fields whose children will inherit the Rename fields and Rename expression operations. Supports wildcards. If empty, only top-level fields will be renamed.
   */
  baseFields?: Array<string> | undefined;
  /**
   * Set of key-value pairs to rename fields, where key is the current name and value is the new name. Does not support internal fields.
   */
  rename?: Array<Rename> | undefined;
  /**
   * Optional JavaScript expression whose returned value will be used to rename fields. Use the 'name' and 'value' global variables to access field names/values. Example: `name.startsWith('data') ? name.toUpperCase() : name`. You can access other field values via __e.<fieldName>.
   */
  renameExpr?: string | undefined;
  /**
   * For wildcards specified in Parent fields, sets the maximum depth within events to match and rename fields. Enter `0` to match only top-level fields. Defaults to `5` levels down.
   */
  wildcardDepth?: number | undefined;
};

export const FunctionRenameId = {
  Rename: "rename",
} as const;
export type FunctionRenameId = ClosedEnum<typeof FunctionRenameId>;

export type FunctionRename = {
  conf: FunctionRenameConf;
  filename: string;
  disabled: boolean;
  group: string;
  id: FunctionRenameId;
  initTime: number;
  loadTime: number;
  modTime: number;
  name: string;
  /**
   * JSON Schema Draft-06
   */
  schema: { [k: string]: any };
  uischema: { [k: string]: any };
  version: string;
};

/** @internal */
export const Rename$inboundSchema: z.ZodType<Rename, z.ZodTypeDef, unknown> = z
  .object({
    currentName: z.string(),
    newName: z.string(),
  });

export function renameFromJSON(
  jsonString: string,
): SafeParseResult<Rename, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Rename$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Rename' from JSON`,
  );
}

/** @internal */
export const FunctionRenameConf$inboundSchema: z.ZodType<
  FunctionRenameConf,
  z.ZodTypeDef,
  unknown
> = z.object({
  baseFields: z.array(z.string()).optional(),
  rename: z.array(z.lazy(() => Rename$inboundSchema)).optional(),
  renameExpr: z.string().optional(),
  wildcardDepth: z.number().int().default(5),
});

export function functionRenameConfFromJSON(
  jsonString: string,
): SafeParseResult<FunctionRenameConf, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FunctionRenameConf$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FunctionRenameConf' from JSON`,
  );
}

/** @internal */
export const FunctionRenameId$inboundSchema: z.ZodNativeEnum<
  typeof FunctionRenameId
> = z.nativeEnum(FunctionRenameId);

/** @internal */
export const FunctionRename$inboundSchema: z.ZodType<
  FunctionRename,
  z.ZodTypeDef,
  unknown
> = z.object({
  __conf: z.lazy(() => FunctionRenameConf$inboundSchema),
  __filename: z.string(),
  disabled: z.boolean(),
  group: z.string(),
  id: FunctionRenameId$inboundSchema,
  initTime: z.number(),
  loadTime: z.number(),
  modTime: z.number(),
  name: z.string(),
  schema: z.record(z.any()),
  uischema: z.record(z.any()),
  version: z.string(),
}).transform((v) => {
  return remap$(v, {
    "__conf": "conf",
    "__filename": "filename",
  });
});

export function functionRenameFromJSON(
  jsonString: string,
): SafeParseResult<FunctionRename, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FunctionRename$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FunctionRename' from JSON`,
  );
}
