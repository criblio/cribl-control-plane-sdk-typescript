/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  MasterWorkerEntry,
  MasterWorkerEntry$inboundSchema,
} from "./masterworkerentry.js";

export type PaginatedMasterWorkerEntry = {
  /**
   * the pre-limited items in the list of results
   */
  items: Array<MasterWorkerEntry>;
  /**
   * number of items present in the items array
   */
  count: number;
  /**
   * pagination offset
   */
  offset: number;
  /**
   * pagination limit
   */
  limit: number;
  /**
   * total number of items available (present when limit is set)
   */
  totalCount?: number | undefined;
};

/** @internal */
export const PaginatedMasterWorkerEntry$inboundSchema: z.ZodType<
  PaginatedMasterWorkerEntry,
  z.ZodTypeDef,
  unknown
> = z.object({
  items: z.array(MasterWorkerEntry$inboundSchema),
  count: types.number(),
  offset: types.number(),
  limit: types.number(),
  totalCount: types.optional(types.number()),
});

export function paginatedMasterWorkerEntryFromJSON(
  jsonString: string,
): SafeParseResult<PaginatedMasterWorkerEntry, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PaginatedMasterWorkerEntry$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PaginatedMasterWorkerEntry' from JSON`,
  );
}
