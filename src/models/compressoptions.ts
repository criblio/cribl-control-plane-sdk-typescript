/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import {
  catchUnrecognizedEnum,
  OpenEnum,
  Unrecognized,
} from "../types/enums.js";

/**
 * Controls whether to support reading compressed data from a forwarder. Select 'Automatic' to match the forwarder's configuration, or 'Disabled' to reject compressed connections.
 */
export const CompressOptions = {
  /**
   * Disabled
   */
  Disabled: "disabled",
  /**
   * Automatic
   */
  Auto: "auto",
  /**
   * Always
   */
  Always: "always",
} as const;
/**
 * Controls whether to support reading compressed data from a forwarder. Select 'Automatic' to match the forwarder's configuration, or 'Disabled' to reject compressed connections.
 */
export type CompressOptions = OpenEnum<typeof CompressOptions>;

/** @internal */
export const CompressOptions$inboundSchema: z.ZodType<
  CompressOptions,
  z.ZodTypeDef,
  unknown
> = z
  .union([
    z.nativeEnum(CompressOptions),
    z.string().transform(catchUnrecognizedEnum),
  ]);
/** @internal */
export const CompressOptions$outboundSchema: z.ZodType<
  CompressOptions,
  z.ZodTypeDef,
  CompressOptions
> = z.union([
  z.nativeEnum(CompressOptions),
  z.string().and(z.custom<Unrecognized<string>>()),
]);
