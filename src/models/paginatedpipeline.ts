/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { Pipeline, Pipeline$inboundSchema } from "./pipeline.js";

export type PaginatedPipeline = {
  /**
   * The pre-limited items in the list of results
   */
  items: Array<Pipeline>;
  /**
   * Number of items present in the items array
   */
  count: number;
  /**
   * Pagination offset
   */
  offset?: number | undefined;
  /**
   * Pagination limit
   */
  limit?: number | undefined;
  /**
   * Total number of items available (present when limit is set)
   */
  totalCount?: number | undefined;
};

/** @internal */
export const PaginatedPipeline$inboundSchema: z.ZodType<
  PaginatedPipeline,
  z.ZodTypeDef,
  unknown
> = z.object({
  items: z.array(Pipeline$inboundSchema),
  count: types.number(),
  offset: types.optional(types.number()),
  limit: types.optional(types.number()),
  totalCount: types.optional(types.number()),
});

export function paginatedPipelineFromJSON(
  jsonString: string,
): SafeParseResult<PaginatedPipeline, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PaginatedPipeline$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PaginatedPipeline' from JSON`,
  );
}
