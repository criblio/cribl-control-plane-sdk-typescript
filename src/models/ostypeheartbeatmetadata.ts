/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type OsTypeHeartbeatMetadata = {
  /**
   * Network addresses reported by the operating system.
   */
  addresses: Array<string>;
  /**
   * If <code>true</code>, the operating system metadata collector is enabled on the node. Otherwise, <code>false</code>.
   */
  enabled: boolean;
  /**
   * Operating system distribution name.
   */
  id: string;
  /**
   * Operating system version.
   */
  version: string;
};

/** @internal */
export const OsTypeHeartbeatMetadata$inboundSchema: z.ZodType<
  OsTypeHeartbeatMetadata,
  z.ZodTypeDef,
  unknown
> = z.object({
  addresses: z.array(types.string()),
  enabled: types.boolean(),
  id: types.string(),
  version: types.string(),
});

export function osTypeHeartbeatMetadataFromJSON(
  jsonString: string,
): SafeParseResult<OsTypeHeartbeatMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OsTypeHeartbeatMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OsTypeHeartbeatMetadata' from JSON`,
  );
}
