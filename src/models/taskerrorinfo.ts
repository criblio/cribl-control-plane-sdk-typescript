/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Serialized error object that describes why a job entered its current <code>state</code>. Includes <code>message</code> and may include a nested <code>reason</code> for wrapped errors.
 */
export type TaskErrorInfo = {
  /**
   * Human-readable error message.
   */
  message: string;
  /**
   * Error name, if available.
   */
  name?: string | undefined;
  /**
   * Truncated stack trace of the error.
   */
  stack?: string | undefined;
  [additionalProperties: string]: unknown;
};

/** @internal */
export const TaskErrorInfo$inboundSchema: z.ZodType<
  TaskErrorInfo,
  z.ZodTypeDef,
  unknown
> = z.object({
  message: types.string(),
  name: types.optional(types.string()),
  stack: types.optional(types.string()),
}).catchall(z.any());

export function taskErrorInfoFromJSON(
  jsonString: string,
): SafeParseResult<TaskErrorInfo, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TaskErrorInfo$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TaskErrorInfo' from JSON`,
  );
}
