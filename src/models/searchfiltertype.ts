/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type SearchFilterType = {
  /**
   * Search filter attribute name, see: https://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_DescribeInstances.html for more information. Attributes can be manually entered if not present in the drop down list
   */
  name: string;
  /**
   * Search Filter Values, if empty only "running" EC2 instances will be returned
   */
  values: Array<string>;
};

/** @internal */
export const SearchFilterType$inboundSchema: z.ZodType<
  SearchFilterType,
  z.ZodTypeDef,
  unknown
> = z.object({
  Name: z.string(),
  Values: z.array(z.string()),
}).transform((v) => {
  return remap$(v, {
    "Name": "name",
    "Values": "values",
  });
});
/** @internal */
export type SearchFilterType$Outbound = {
  Name: string;
  Values: Array<string>;
};

/** @internal */
export const SearchFilterType$outboundSchema: z.ZodType<
  SearchFilterType$Outbound,
  z.ZodTypeDef,
  SearchFilterType
> = z.object({
  name: z.string(),
  values: z.array(z.string()),
}).transform((v) => {
  return remap$(v, {
    name: "Name",
    values: "Values",
  });
});

export function searchFilterTypeToJSON(
  searchFilterType: SearchFilterType,
): string {
  return JSON.stringify(
    SearchFilterType$outboundSchema.parse(searchFilterType),
  );
}
export function searchFilterTypeFromJSON(
  jsonString: string,
): SafeParseResult<SearchFilterType, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchFilterType$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchFilterType' from JSON`,
  );
}
