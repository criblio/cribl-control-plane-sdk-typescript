/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type LogLabelsType = {
  /**
   * Label name
   */
  label: string;
  /**
   * JavaScript expression to compute the label's value.
   */
  valueExpression: string;
};

/** @internal */
export const LogLabelsType$inboundSchema: z.ZodType<
  LogLabelsType,
  z.ZodTypeDef,
  unknown
> = z.object({
  label: z.string(),
  valueExpression: z.string(),
});
/** @internal */
export type LogLabelsType$Outbound = {
  label: string;
  valueExpression: string;
};

/** @internal */
export const LogLabelsType$outboundSchema: z.ZodType<
  LogLabelsType$Outbound,
  z.ZodTypeDef,
  LogLabelsType
> = z.object({
  label: z.string(),
  valueExpression: z.string(),
});

export function logLabelsTypeToJSON(logLabelsType: LogLabelsType): string {
  return JSON.stringify(LogLabelsType$outboundSchema.parse(logLabelsType));
}
export function logLabelsTypeFromJSON(
  jsonString: string,
): SafeParseResult<LogLabelsType, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LogLabelsType$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LogLabelsType' from JSON`,
  );
}
