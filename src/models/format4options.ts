/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import {
  catchUnrecognizedEnum,
  OpenEnum,
  Unrecognized,
} from "../types/enums.js";

/**
 * When set to JSON, the event is automatically formatted with required fields before sending. When set to Raw, only the event's `_raw` value is sent.
 */
export const Format4Options = {
  /**
   * JSON
   */
  Json: "JSON",
  /**
   * Raw
   */
  Raw: "raw",
} as const;
/**
 * When set to JSON, the event is automatically formatted with required fields before sending. When set to Raw, only the event's `_raw` value is sent.
 */
export type Format4Options = OpenEnum<typeof Format4Options>;

/** @internal */
export const Format4Options$inboundSchema: z.ZodType<
  Format4Options,
  z.ZodTypeDef,
  unknown
> = z
  .union([
    z.nativeEnum(Format4Options),
    z.string().transform(catchUnrecognizedEnum),
  ]);
/** @internal */
export const Format4Options$outboundSchema: z.ZodType<
  Format4Options,
  z.ZodTypeDef,
  Format4Options
> = z.union([
  z.nativeEnum(Format4Options),
  z.string().and(z.custom<Unrecognized<string>>()),
]);
