/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type FunctionCloneConf = {
  /**
   * Create clones with the following fields set
   */
  clones?: Array<{ [k: string]: string }> | undefined;
};

export const FunctionCloneId = {
  Clone: "clone",
} as const;
export type FunctionCloneId = ClosedEnum<typeof FunctionCloneId>;

export type FunctionClone = {
  conf: FunctionCloneConf;
  filename: string;
  disabled: boolean;
  group: string;
  id: FunctionCloneId;
  initTime: number;
  loadTime: number;
  modTime: number;
  name: string;
  /**
   * JSON Schema Draft-06
   */
  schema: { [k: string]: any };
  uischema: { [k: string]: any };
  version: string;
};

/** @internal */
export const FunctionCloneConf$inboundSchema: z.ZodType<
  FunctionCloneConf,
  z.ZodTypeDef,
  unknown
> = z.object({
  clones: z.array(z.record(z.string())).optional(),
});

export function functionCloneConfFromJSON(
  jsonString: string,
): SafeParseResult<FunctionCloneConf, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FunctionCloneConf$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FunctionCloneConf' from JSON`,
  );
}

/** @internal */
export const FunctionCloneId$inboundSchema: z.ZodNativeEnum<
  typeof FunctionCloneId
> = z.nativeEnum(FunctionCloneId);

/** @internal */
export const FunctionClone$inboundSchema: z.ZodType<
  FunctionClone,
  z.ZodTypeDef,
  unknown
> = z.object({
  __conf: z.lazy(() => FunctionCloneConf$inboundSchema),
  __filename: z.string(),
  disabled: z.boolean(),
  group: z.string(),
  id: FunctionCloneId$inboundSchema,
  initTime: z.number(),
  loadTime: z.number(),
  modTime: z.number(),
  name: z.string(),
  schema: z.record(z.any()),
  uischema: z.record(z.any()),
  version: z.string(),
}).transform((v) => {
  return remap$(v, {
    "__conf": "conf",
    "__filename": "filename",
  });
});

export function functionCloneFromJSON(
  jsonString: string,
): SafeParseResult<FunctionClone, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FunctionClone$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FunctionClone' from JSON`,
  );
}
