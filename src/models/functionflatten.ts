/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type FunctionFlattenConf = {
  /**
   * List of top-level fields to include for flattening. Supports * wildcards, except when used on internal fields. Defaults to empty array, which means all fields.
   */
  fields?: Array<string> | undefined;
  /**
   * Prefix string for flattened field names. Defaults to empty.
   */
  prefix?: string | undefined;
  /**
   * Number representing the nested levels to consider for flattening. Defaults to 5. Minimum should be 1.
   */
  depth?: number | undefined;
  /**
   * Delimiter to be used for flattening. Defaults to underscore.
   */
  delimiter?: string | undefined;
};

export const FunctionFlattenId = {
  Flatten: "flatten",
} as const;
export type FunctionFlattenId = ClosedEnum<typeof FunctionFlattenId>;

export type FunctionFlatten = {
  conf: FunctionFlattenConf;
  filename: string;
  disabled: boolean;
  group: string;
  id: FunctionFlattenId;
  initTime: number;
  loadTime: number;
  modTime: number;
  name: string;
  /**
   * JSON Schema Draft-06
   */
  schema: { [k: string]: any };
  uischema: { [k: string]: any };
  version: string;
};

/** @internal */
export const FunctionFlattenConf$inboundSchema: z.ZodType<
  FunctionFlattenConf,
  z.ZodTypeDef,
  unknown
> = z.object({
  fields: z.array(z.string()).optional(),
  prefix: z.string().default(""),
  depth: z.number().default(5),
  delimiter: z.string().default("_"),
});

export function functionFlattenConfFromJSON(
  jsonString: string,
): SafeParseResult<FunctionFlattenConf, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FunctionFlattenConf$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FunctionFlattenConf' from JSON`,
  );
}

/** @internal */
export const FunctionFlattenId$inboundSchema: z.ZodNativeEnum<
  typeof FunctionFlattenId
> = z.nativeEnum(FunctionFlattenId);

/** @internal */
export const FunctionFlatten$inboundSchema: z.ZodType<
  FunctionFlatten,
  z.ZodTypeDef,
  unknown
> = z.object({
  __conf: z.lazy(() => FunctionFlattenConf$inboundSchema),
  __filename: z.string(),
  disabled: z.boolean(),
  group: z.string(),
  id: FunctionFlattenId$inboundSchema,
  initTime: z.number(),
  loadTime: z.number(),
  modTime: z.number(),
  name: z.string(),
  schema: z.record(z.any()),
  uischema: z.record(z.any()),
  version: z.string(),
}).transform((v) => {
  return remap$(v, {
    "__conf": "conf",
    "__filename": "filename",
  });
});

export function functionFlattenFromJSON(
  jsonString: string,
): SafeParseResult<FunctionFlatten, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FunctionFlatten$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FunctionFlatten' from JSON`,
  );
}
