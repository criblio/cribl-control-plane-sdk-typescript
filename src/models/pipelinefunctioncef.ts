/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  FunctionConfSchemaCef,
  FunctionConfSchemaCef$inboundSchema,
} from "./functionconfschemacef.js";

export type PipelineFunctionCef = {
  /**
   * JavaScript expression that selects data to pass through the Function.
   */
  filter?: string | undefined;
  /**
   * Identifier of the Function. Always <code>cef</code>
   */
  id: "cef";
  /**
   * Brief description of the Pipeline function.
   */
  description?: string | undefined;
  /**
   * If <code>true</code>, disable the Pipeline function so that events are not passed through it. Otherwise, <code>false</code>.
   */
  disabled?: boolean | undefined;
  /**
   * If <code>true</code>, stop passing events to downstream Pipeline Functions after the Function executes. Otherwise, <code>false</code>.
   */
  final?: boolean | undefined;
  conf: FunctionConfSchemaCef;
  /**
   * Unique identifier of the group that contains the Pipeline Function.
   */
  groupId?: string | undefined;
};

/** @internal */
export const PipelineFunctionCef$inboundSchema: z.ZodType<
  PipelineFunctionCef,
  z.ZodTypeDef,
  unknown
> = z.object({
  filter: types.optional(types.string()),
  id: types.literal("cef"),
  description: types.optional(types.string()),
  disabled: types.optional(types.boolean()),
  final: types.optional(types.boolean()),
  conf: FunctionConfSchemaCef$inboundSchema,
  groupId: types.optional(types.string()),
});

export function pipelineFunctionCefFromJSON(
  jsonString: string,
): SafeParseResult<PipelineFunctionCef, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PipelineFunctionCef$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PipelineFunctionCef' from JSON`,
  );
}
