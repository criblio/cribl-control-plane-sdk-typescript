/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type RunSettings = {};

/**
 * Configuration for a scheduled job
 */
export type ScheduleType = {
  /**
   * Enable to configure scheduling for this Collector
   */
  enabled?: boolean | undefined;
  /**
   * Skippable jobs can be delayed, up to their next run time, if the system is hitting concurrency limits
   */
  skippable?: boolean | undefined;
  /**
   * If Stream Leader (or single instance) restarts, run all missed jobs according to their original schedules
   */
  resumeMissed?: boolean | undefined;
  /**
   * A cron schedule on which to run this job
   */
  cronSchedule?: string | undefined;
  /**
   * The maximum number of instances of this scheduled job that may be running at any time
   */
  maxConcurrentRuns?: number | undefined;
  run?: RunSettings | undefined;
};

/** @internal */
export const RunSettings$inboundSchema: z.ZodType<
  RunSettings,
  z.ZodTypeDef,
  unknown
> = z.object({});

export function runSettingsFromJSON(
  jsonString: string,
): SafeParseResult<RunSettings, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RunSettings$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RunSettings' from JSON`,
  );
}

/** @internal */
export const ScheduleType$inboundSchema: z.ZodType<
  ScheduleType,
  z.ZodTypeDef,
  unknown
> = z.object({
  enabled: z.boolean().optional(),
  skippable: z.boolean().default(true),
  resumeMissed: z.boolean().default(false),
  cronSchedule: z.string().default("*/5 * * * *"),
  maxConcurrentRuns: z.number().default(1),
  run: z.lazy(() => RunSettings$inboundSchema).optional(),
});

export function scheduleTypeFromJSON(
  jsonString: string,
): SafeParseResult<ScheduleType, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduleType$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduleType' from JSON`,
  );
}
