/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { GitInfo, GitInfo$inboundSchema } from "./gitinfo.js";

export type CountedGitInfo = {
  /**
   * number of items present in the items array
   */
  count?: number | undefined;
  items?: Array<GitInfo> | undefined;
};

/** @internal */
export const CountedGitInfo$inboundSchema: z.ZodType<
  CountedGitInfo,
  z.ZodTypeDef,
  unknown
> = z.object({
  count: z.number().int().optional(),
  items: z.array(GitInfo$inboundSchema).optional(),
});

export function countedGitInfoFromJSON(
  jsonString: string,
): SafeParseResult<CountedGitInfo, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CountedGitInfo$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CountedGitInfo' from JSON`,
  );
}
