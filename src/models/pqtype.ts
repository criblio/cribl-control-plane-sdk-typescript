/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import {
  catchUnrecognizedEnum,
  OpenEnum,
  Unrecognized,
} from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * With Smart mode, PQ will write events to the filesystem only when it detects backpressure from the processing engine. With Always On mode, PQ will always write events directly to the queue before forwarding them to the processing engine.
 */
export const PqTypeMode = {
  /**
   * Smart
   */
  Smart: "smart",
  /**
   * Always On
   */
  Always: "always",
} as const;
/**
 * With Smart mode, PQ will write events to the filesystem only when it detects backpressure from the processing engine. With Always On mode, PQ will always write events directly to the queue before forwarding them to the processing engine.
 */
export type PqTypeMode = OpenEnum<typeof PqTypeMode>;

/**
 * Codec to use to compress the persisted data
 */
export const PqTypeCompression = {
  /**
   * None
   */
  None: "none",
  /**
   * Gzip
   */
  Gzip: "gzip",
} as const;
/**
 * Codec to use to compress the persisted data
 */
export type PqTypeCompression = OpenEnum<typeof PqTypeCompression>;

export type PqControls = {};

export type PqType = {
  /**
   * With Smart mode, PQ will write events to the filesystem only when it detects backpressure from the processing engine. With Always On mode, PQ will always write events directly to the queue before forwarding them to the processing engine.
   */
  mode?: PqTypeMode | undefined;
  /**
   * The maximum number of events to hold in memory before writing the events to disk
   */
  maxBufferSize?: number | undefined;
  /**
   * The number of events to send downstream before committing that Stream has read them
   */
  commitFrequency?: number | undefined;
  /**
   * The maximum size to store in each queue file before closing and optionally compressing. Enter a numeral with units of KB, MB, etc.
   */
  maxFileSize?: string | undefined;
  /**
   * The maximum disk space that the queue can consume (as an average per Worker Process) before queueing stops. Enter a numeral with units of KB, MB, etc.
   */
  maxSize?: string | undefined;
  /**
   * The location for the persistent queue files. To this field's value, the system will append: /<worker-id>/inputs/<input-id>
   */
  path?: string | undefined;
  /**
   * Codec to use to compress the persisted data
   */
  compress?: PqTypeCompression | undefined;
  pqControls?: PqControls | undefined;
};

/** @internal */
export const PqTypeMode$inboundSchema: z.ZodType<
  PqTypeMode,
  z.ZodTypeDef,
  unknown
> = z
  .union([
    z.nativeEnum(PqTypeMode),
    z.string().transform(catchUnrecognizedEnum),
  ]);
/** @internal */
export const PqTypeMode$outboundSchema: z.ZodType<
  PqTypeMode,
  z.ZodTypeDef,
  PqTypeMode
> = z.union([
  z.nativeEnum(PqTypeMode),
  z.string().and(z.custom<Unrecognized<string>>()),
]);

/** @internal */
export const PqTypeCompression$inboundSchema: z.ZodType<
  PqTypeCompression,
  z.ZodTypeDef,
  unknown
> = z
  .union([
    z.nativeEnum(PqTypeCompression),
    z.string().transform(catchUnrecognizedEnum),
  ]);
/** @internal */
export const PqTypeCompression$outboundSchema: z.ZodType<
  PqTypeCompression,
  z.ZodTypeDef,
  PqTypeCompression
> = z.union([
  z.nativeEnum(PqTypeCompression),
  z.string().and(z.custom<Unrecognized<string>>()),
]);

/** @internal */
export const PqControls$inboundSchema: z.ZodType<
  PqControls,
  z.ZodTypeDef,
  unknown
> = z.object({});
/** @internal */
export type PqControls$Outbound = {};

/** @internal */
export const PqControls$outboundSchema: z.ZodType<
  PqControls$Outbound,
  z.ZodTypeDef,
  PqControls
> = z.object({});

export function pqControlsToJSON(pqControls: PqControls): string {
  return JSON.stringify(PqControls$outboundSchema.parse(pqControls));
}
export function pqControlsFromJSON(
  jsonString: string,
): SafeParseResult<PqControls, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PqControls$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PqControls' from JSON`,
  );
}

/** @internal */
export const PqType$inboundSchema: z.ZodType<PqType, z.ZodTypeDef, unknown> = z
  .object({
    mode: PqTypeMode$inboundSchema.default("always"),
    maxBufferSize: z.number().default(1000),
    commitFrequency: z.number().default(42),
    maxFileSize: z.string().default("1 MB"),
    maxSize: z.string().default("5GB"),
    path: z.string().default("$CRIBL_HOME/state/queues"),
    compress: PqTypeCompression$inboundSchema.default("none"),
    pqControls: z.lazy(() => PqControls$inboundSchema).optional(),
  });
/** @internal */
export type PqType$Outbound = {
  mode: string;
  maxBufferSize: number;
  commitFrequency: number;
  maxFileSize: string;
  maxSize: string;
  path: string;
  compress: string;
  pqControls?: PqControls$Outbound | undefined;
};

/** @internal */
export const PqType$outboundSchema: z.ZodType<
  PqType$Outbound,
  z.ZodTypeDef,
  PqType
> = z.object({
  mode: PqTypeMode$outboundSchema.default("always"),
  maxBufferSize: z.number().default(1000),
  commitFrequency: z.number().default(42),
  maxFileSize: z.string().default("1 MB"),
  maxSize: z.string().default("5GB"),
  path: z.string().default("$CRIBL_HOME/state/queues"),
  compress: PqTypeCompression$outboundSchema.default("none"),
  pqControls: z.lazy(() => PqControls$outboundSchema).optional(),
});

export function pqTypeToJSON(pqType: PqType): string {
  return JSON.stringify(PqType$outboundSchema.parse(pqType));
}
export function pqTypeFromJSON(
  jsonString: string,
): SafeParseResult<PqType, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PqType$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PqType' from JSON`,
  );
}
