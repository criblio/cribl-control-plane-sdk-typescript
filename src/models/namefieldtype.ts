/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { smartUnion } from "../types/smartUnion.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type Path = string | number;

export type NameFieldType = {
  raw: string;
  path: Array<string | number>;
};

/** @internal */
export const Path$inboundSchema: z.ZodType<Path, z.ZodTypeDef, unknown> =
  smartUnion([types.string(), types.number()]);
/** @internal */
export type Path$Outbound = string | number;

/** @internal */
export const Path$outboundSchema: z.ZodType<Path$Outbound, z.ZodTypeDef, Path> =
  smartUnion([z.string(), z.number()]);

export function pathToJSON(path: Path): string {
  return JSON.stringify(Path$outboundSchema.parse(path));
}
export function pathFromJSON(
  jsonString: string,
): SafeParseResult<Path, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Path$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Path' from JSON`,
  );
}

/** @internal */
export const NameFieldType$inboundSchema: z.ZodType<
  NameFieldType,
  z.ZodTypeDef,
  unknown
> = z.object({
  raw: types.string(),
  path: z.array(smartUnion([types.string(), types.number()])),
});
/** @internal */
export type NameFieldType$Outbound = {
  raw: string;
  path: Array<string | number>;
};

/** @internal */
export const NameFieldType$outboundSchema: z.ZodType<
  NameFieldType$Outbound,
  z.ZodTypeDef,
  NameFieldType
> = z.object({
  raw: z.string(),
  path: z.array(smartUnion([z.string(), z.number()])),
});

export function nameFieldTypeToJSON(nameFieldType: NameFieldType): string {
  return JSON.stringify(NameFieldType$outboundSchema.parse(nameFieldType));
}
export function nameFieldTypeFromJSON(
  jsonString: string,
): SafeParseResult<NameFieldType, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => NameFieldType$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'NameFieldType' from JSON`,
  );
}
