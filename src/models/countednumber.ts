/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type CountedNumber = {
  /**
   * number of items present in the items array
   */
  count: number;
  /**
   * List of items in this response.
   */
  items: Array<number>;
};

/** @internal */
export const CountedNumber$inboundSchema: z.ZodType<
  CountedNumber,
  z.ZodTypeDef,
  unknown
> = z.object({
  count: types.number(),
  items: z.array(types.number()),
});

export function countedNumberFromJSON(
  jsonString: string,
): SafeParseResult<CountedNumber, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CountedNumber$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CountedNumber' from JSON`,
  );
}
