# SortDirection

Used only when Sort by field is set.

## Example Usage

```typescript
import { SortDirection } from "cribl-control-plane/models";

let value: SortDirection = "asc";

// Open enum: unrecognized values are captured as Unrecognized<string>
```

## Values

```typescript
"asc" | "desc" | Unrecognized<string>
```