# SendToRoutesTrueConstraintMode

Choose how to discover files to monitor

## Example Usage

```typescript
import { SendToRoutesTrueConstraintMode } from "cribl-control-plane/models";

let value: SendToRoutesTrueConstraintMode = "manual";
```

## Values

This is an open enum. Unrecognized values will be captured as the `Unrecognized<string>` branded type.

```typescript
"manual" | "auto" | Unrecognized<string>
```