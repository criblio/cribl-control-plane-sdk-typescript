# OutputLokiAuthenticationType8

## Example Usage

```typescript
import { OutputLokiAuthenticationType8 } from "cribl-control-plane/models";

let value: OutputLokiAuthenticationType8 = "textSecret";
```

## Values

This is an open enum. Unrecognized values will be captured as the `Unrecognized<string>` branded type.

```typescript
"none" | "token" | "textSecret" | "basic" | "credentialsSecret" | Unrecognized<string>
```