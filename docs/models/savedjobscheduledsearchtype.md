# SavedJobScheduledSearchType

## Example Usage

```typescript
import { SavedJobScheduledSearchType } from "cribl-control-plane/models";

let value: SavedJobScheduledSearchType = "collection";
```

## Values

This is an open enum. Unrecognized values will be captured as the `Unrecognized<string>` branded type.

```typescript
"collection" | Unrecognized<string>
```