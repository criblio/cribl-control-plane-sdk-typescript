# InputCollectionPart0TypeMode

Choose how to discover files to monitor

## Example Usage

```typescript
import { InputCollectionPart0TypeMode } from "cribl-control-plane/models";

let value: InputCollectionPart0TypeMode = "manual";
```

## Values

This is an open enum. Unrecognized values will be captured as the `Unrecognized<string>` branded type.

```typescript
"manual" | "auto" | Unrecognized<string>
```