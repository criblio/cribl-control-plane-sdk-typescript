# SavedJobExecutorJobType

## Example Usage

```typescript
import { SavedJobExecutorJobType } from "cribl-control-plane/models";

let value: SavedJobExecutorJobType = "collection";
```

## Values

This is an open enum. Unrecognized values will be captured as the `Unrecognized<string>` branded type.

```typescript
"collection" | "executor" | "scheduledSearch" | Unrecognized<string>
```