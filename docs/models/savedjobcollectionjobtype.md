# SavedJobCollectionJobType

## Example Usage

```typescript
import { SavedJobCollectionJobType } from "cribl-control-plane/models";

let value: SavedJobCollectionJobType = "scheduledSearch";
```

## Values

This is an open enum. Unrecognized values will be captured as the `Unrecognized<string>` branded type.

```typescript
"collection" | "executor" | "scheduledSearch" | Unrecognized<string>
```