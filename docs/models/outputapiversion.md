# OutputAPIVersion

## Example Usage

```typescript
import { OutputAPIVersion } from "cribl-control-plane/models";

let value: OutputAPIVersion = "v1";
```

## Values

This is an open enum. Unrecognized values will be captured as the `Unrecognized<string>` branded type.

```typescript
"v1" | "v2" | Unrecognized<string>
```