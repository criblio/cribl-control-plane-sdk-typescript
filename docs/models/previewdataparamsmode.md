# PreviewDataParamsMode

## Example Usage

```typescript
import { PreviewDataParamsMode } from "cribl-control-plane/models";

let value: PreviewDataParamsMode = "pipe";
```

## Values

This is an open enum. Unrecognized values will be captured as the `Unrecognized<string>` branded type.

```typescript
"pipe" | "route" | "routeAndSend" | Unrecognized<string>
```