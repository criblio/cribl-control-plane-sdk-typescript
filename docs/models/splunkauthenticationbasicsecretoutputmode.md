# SplunkAuthenticationBasicSecretOutputMode

Format of the returned output

## Example Usage

```typescript
import { SplunkAuthenticationBasicSecretOutputMode } from "cribl-control-plane/models";

let value: SplunkAuthenticationBasicSecretOutputMode = "csv";
```

## Values

This is an open enum. Unrecognized values will be captured as the `Unrecognized<string>` branded type.

```typescript
"csv" | "json" | Unrecognized<string>
```