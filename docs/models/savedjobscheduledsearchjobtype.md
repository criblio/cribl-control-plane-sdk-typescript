# SavedJobScheduledSearchJobType

## Example Usage

```typescript
import { SavedJobScheduledSearchJobType } from "cribl-control-plane/models";

let value: SavedJobScheduledSearchJobType = "collection";
```

## Values

This is an open enum. Unrecognized values will be captured as the `Unrecognized<string>` branded type.

```typescript
"collection" | "executor" | "scheduledSearch" | Unrecognized<string>
```